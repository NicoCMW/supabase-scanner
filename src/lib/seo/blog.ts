import type { BlogPost } from "./types";

const posts: readonly BlogPost[] = [
  {
    slug: "state-of-supabase-security-2026",
    title: "The State of Supabase Security in 2026",
    description:
      "How secure are Supabase applications in 2026? We analyzed common vulnerability patterns across Supabase projects and found that most apps share the same critical misconfigurations.",
    category: "Security Basics",
    publishedAt: "2026-03-15",
    keywords: [
      "supabase security",
      "supabase security audit",
      "is supabase secure",
      "supabase RLS",
    ],
    body: "<p>Supabase has become the default database for a generation of developers. It powers side projects, startups, and increasingly, production SaaS applications serving real users with real data. Its developer experience is unmatched: spin up a Postgres database, get instant REST and GraphQL APIs, add authentication in minutes, and deploy.</p>\n\n<p>But there is a gap between how easy Supabase makes it to build and how much security knowledge it assumes you have. And that gap is widening.</p>\n\n<p>In 2026, three forces are converging to make Supabase security a critical topic:</p>\n\n<ol>\n<li><strong>Supabase adoption is accelerating.</strong> The platform has grown from a developer tool into infrastructure that runs real businesses.</li>\n<li><strong>Vibe coding has gone mainstream.</strong> Developers are shipping AI-generated code faster than ever, often without reviewing the security implications.</li>\n<li><strong>Attackers have noticed.</strong> The same API patterns that make Supabase great for developers make misconfigured projects easy to discover and exploit.</li>\n</ol>\n\n<p>This article examines the current state of Supabase security: what is going well, what is going wrong, and what every developer using Supabase should know heading into the rest of 2026.</p>\n\n<h3>The Security Model: Powerful but Misunderstood</h3>\n\n<p>Supabase's security model is built on three pillars:</p>\n\n<p><strong>Row Level Security (RLS):</strong> Postgres-native policies that control which rows each user can read, write, update, or delete. RLS is the primary access control mechanism for Supabase's auto-generated APIs. If you are unfamiliar with common RLS pitfalls, read our guide on <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<p><strong>API Key Separation:</strong> Two keys with fundamentally different privilege levels. The <code>anon</code> key respects RLS policies. The <code>service_role</code> key bypasses them entirely.</p>\n\n<p><strong>Authentication:</strong> Built-in auth with email/password, OAuth providers, magic links, and phone authentication. JWTs carry user claims that RLS policies evaluate.</p>\n\n<p>This is a strong foundation. Supabase's approach of leveraging Postgres's built-in security features rather than inventing a proprietary system means you get battle-tested access control backed by decades of Postgres development.</p>\n\n<p>But here is the problem: the model only works when developers configure it correctly. And the evidence suggests that many do not.</p>\n\n<h3>The RLS Problem</h3>\n\n<p>Row Level Security is Supabase's most important security feature and its most commonly misconfigured one.</p>\n\n<p>The core issue is straightforward. When you create a new table in Supabase, RLS is now enabled by default (a welcome change made in late 2025). But enabling RLS is only half the equation. Without policies defining who can access what, an RLS-enabled table with no policies is completely inaccessible -- which means developers quickly learn to either disable RLS temporarily or write overly permissive policies to get their application working.</p>\n\n<p>Both paths lead to vulnerabilities.</p>\n\n<p><strong>Pattern 1: RLS disabled entirely.</strong> The developer disables RLS during development and forgets to re-enable it before deploying. Every row in the table is accessible to anyone with the project URL and anon key -- both of which are embedded in frontend code and trivially extractable.</p>\n\n<p><strong>Pattern 2: Overly permissive policies.</strong> The developer writes a policy like <code>CREATE POLICY \"allow all\" ON public.users FOR ALL USING (true)</code> because it makes the application work. This is functionally equivalent to having no RLS at all.</p>\n\n<p><strong>Pattern 3: Incomplete policy coverage.</strong> The developer writes a SELECT policy but forgets that UPDATE and DELETE operations need their own policies. Or they write a policy using <code>USING</code> but forget that INSERT operations require <code>WITH CHECK</code>.</p>\n\n<p>These are not hypothetical scenarios. They are the most common patterns found in real Supabase applications. For a detailed guide on fixing these issues, see <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>. We break down each of these mistakes in detail in <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<h3>The Vibe Coding Amplifier</h3>\n\n<p>The rise of vibe coding -- prompt-driven development where AI generates the code -- has amplified these security patterns. When a developer tells an AI assistant to add a comments table to their Supabase project, the generated code typically creates the table, sets up the schema, and writes the client-side queries. What it often does not do:</p>\n\n<ul>\n<li>Enable RLS with appropriate policies</li>\n<li>Validate that the anon key is not being used for operations that require the service role</li>\n<li>Check whether storage buckets are set to public or private</li>\n<li>Implement proper input validation before database operations</li>\n</ul>\n\n<p>Research from Veracode found that nearly half of AI-generated code contains known vulnerabilities. The Moltbook incident in February 2026 -- where a social networking site built entirely through vibe coding exposed 1.5 million authentication tokens -- demonstrated what happens when this code reaches production.</p>\n\n<p>The issue is not that AI generates bad code. The issue is that AI generates code optimized for functionality, not security. And most developers accepting that code lack the security expertise to identify what is missing.</p>\n\n<h3>Storage: The Overlooked Attack Surface</h3>\n\n<p>While RLS gets most of the security attention, Supabase Storage is quietly becoming a significant attack surface. We wrote a deep-dive on this topic: <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>. Many applications store sensitive files -- user uploads, documents, profile photos with metadata -- in public buckets because it is simpler.</p>\n\n<p>Public bucket URLs follow a predictable pattern, making enumeration trivial. We cover this in depth in <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>, including how to check your buckets and migrate to secure access patterns.</p>\n\n<h3>API Key Exposure</h3>\n\n<p>Every Supabase project has two API keys that serve fundamentally different purposes:</p>\n\n<ul>\n<li><strong><code>anon</code> key:</strong> Safe to use in frontend code. All requests are filtered through RLS policies.</li>\n<li><strong><code>service_role</code> key:</strong> Bypasses all RLS. Intended exclusively for server-side use.</li>\n</ul>\n\n<p>The most dangerous misconfiguration is using the <code>service_role</code> key in client-side code. This gives every user of your application full, unrestricted access to your entire database. No RLS policies apply. No row-level restrictions matter.</p>\n\n<p>This happens more often than you might expect, particularly in AI-generated code where the model reaches for the key that works without distinguishing between frontend and backend contexts.</p>\n\n<h3>What Supabase Is Doing Right</h3>\n\n<p>It is worth noting that Supabase has been making significant security improvements:</p>\n\n<ul>\n<li><strong>RLS enabled by default</strong> on new tables (late 2025)</li>\n<li><strong>Security Advisor</strong> in the dashboard that flags common misconfigurations</li>\n<li><strong>Column-level security</strong> adding another layer of data protection</li>\n<li><strong>Option to disable the Data API</strong> entirely for projects that do not need auto-generated endpoints</li>\n<li><strong>SOC 2 Type 2 compliance</strong> with regular third-party audits</li>\n<li><strong>Active HackerOne program</strong> for vulnerability disclosure</li>\n<li><strong>Push protection and grant toggles</strong> on the 2026 roadmap</li>\n</ul>\n\n<p>These are meaningful improvements. But they address the platform side of security. The developer side -- how applications are built on top of Supabase -- remains the primary source of vulnerabilities.</p>\n\n<h3>What You Should Do Today</h3>\n\n<p>If you are building on Supabase, here is your minimum security checklist:</p>\n\n<ol>\n<li><strong>Audit every table's RLS status.</strong> Run <code>SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public';</code> in the SQL Editor. Every table should show <code>true</code>.</li>\n<li><strong>Review your RLS policies.</strong> For each table, verify that policies exist for every operation your application performs. Pay special attention to UPDATE and DELETE policies.</li>\n<li><strong>Check your API key usage.</strong> Search your frontend codebase for your <code>service_role</code> key. If you find it, you have a critical vulnerability. Replace it with the <code>anon</code> key and add appropriate RLS policies.</li>\n<li><strong>Audit your storage buckets.</strong> Review whether each bucket needs to be public. If files contain user data, <a href=\"/blog/supabase-storage-buckets-probably-public\">switch to private buckets with signed URLs</a>.</li>\n<li><strong>Enable email verification</strong> if you use email/password auth. Without it, anyone can create accounts with unverified email addresses.</li>\n<li><strong>Run the Supabase Security Advisor</strong> in your project dashboard. Address every finding.</li>\n<li><strong>Automate your security scanning.</strong> Manual audits are point-in-time. <a href=\"/login\">Run a free scan of your Supabase project</a> to catch misconfigurations as your codebase evolves.</li>\n</ol>\n\n<h3>Looking Ahead</h3>\n\n<p>Supabase security is not a solved problem. The platform provides strong primitives, but the gap between available security features and correctly configured security is where vulnerabilities live.</p>\n\n<p>As Supabase adoption grows and vibe coding becomes the default development workflow, the need for automated security validation will only increase. The developers building on Supabase are not security experts, and they should not have to be. But they do need tools that catch the misconfigurations before attackers do.</p>\n\n<p>The state of Supabase security in 2026 is this: the platform is more secure than ever. The applications built on it, on average, are not.</p>\n\n<p>That is the gap we need to close.</p>\n\n<p><strong><a href=\"/login\">Scan your Supabase project for free --></a></strong></p>",
  },
  {
    slug: "5-rls-mistakes-every-vibe-coder-makes",
    title: "5 RLS Mistakes Every Vibe Coder Makes",
    description:
      "Row Level Security is your last line of defense in Supabase. These are the five most common RLS mistakes we see in AI-generated code and how to fix each one.",
    category: "RLS",
    publishedAt: "2026-03-18",
    keywords: [
      "supabase RLS",
      "supabase RLS mistakes",
      "row level security",
      "vibe coding security",
    ],
    body: "<p>You asked your AI assistant to build a Supabase app. It generated the schema, wrote the queries, wired up the frontend. Everything works.</p>\n\n<p>But does it work <em>securely</em>?</p>\n\n<p>Row Level Security (RLS) is the mechanism that stands between your Supabase data and anyone with a browser. As we covered in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>, RLS misconfiguration is the most common vulnerability pattern. When configured correctly, it ensures users only see and modify their own data. When misconfigured, it is the same as having no security at all.</p>\n\n<p>Here are the five RLS mistakes we see repeatedly in vibe-coded Supabase applications, and the exact SQL to fix each one. For broader context on the Supabase security landscape, see <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>.</p>\n\n<h3>Mistake 1: RLS Is Not Enabled</h3>\n\n<p><strong>What happened:</strong> Your AI assistant created tables using SQL migrations or the Supabase client, and RLS was never enabled. As of late 2025, Supabase enables RLS by default on new tables -- but if your project predates this change, or if your AI generated raw SQL that explicitly sets <code>ALTER TABLE ... DISABLE ROW LEVEL SECURITY</code>, your tables are wide open.</p>\n\n<p><strong>Why it matters:</strong> Without RLS, anyone with your project URL and <code>anon</code> key -- both of which are in your frontend JavaScript -- can read, write, update, and delete every row in the table. Your entire database is one <code>curl</code> command away from being dumped.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT schemaname, tablename, rowsecurity\nFROM pg_tables\nWHERE schemaname = 'public';</code></pre>\n\n<p>Every table should show <code>rowsecurity = true</code>.</p>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">ALTER TABLE public.your_table_name ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.your_table_name FORCE ROW LEVEL SECURITY;</code></pre>\n\n<p>Run this for every table in your public schema. No exceptions.</p>\n\n<h3>Mistake 2: RLS Is Enabled but There Are No Policies</h3>\n\n<p><strong>What happened:</strong> RLS is enabled (good), but no policies were created (bad). This is the most confusing misconfiguration because your application appears broken -- all queries return empty results -- but there are no error messages.</p>\n\n<p><strong>Why it matters:</strong> The developer's instinct when faced with empty query results is to disable RLS. We have seen this pattern repeatedly: enable RLS, see empty results, panic, disable RLS, move on. The temporary fix becomes permanent.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT t.tablename\nFROM pg_tables t\nLEFT JOIN pg_policies p ON t.tablename = p.tablename AND t.schemaname = p.schemaname\nWHERE t.schemaname = 'public'\n  AND t.rowsecurity = true\nGROUP BY t.tablename\nHAVING COUNT(p.policyname) = 0;</code></pre>\n\n<p><strong>The fix:</strong> Add policies appropriate for your use case. Here is a common pattern for a user-owned data table:</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;Users can view own data&quot;\n  ON public.user_data FOR SELECT\n  USING (auth.uid() = user_id);\n\n<p>CREATE POLICY &quot;Users can insert own data&quot;   ON public.user_data FOR INSERT   WITH CHECK (auth.uid() = user_id);</p>\n\n<p>CREATE POLICY &quot;Users can update own data&quot;   ON public.user_data FOR UPDATE   USING (auth.uid() = user_id)   WITH CHECK (auth.uid() = user_id);</p>\n\n<p>CREATE POLICY &quot;Users can delete own data&quot;   ON public.user_data FOR DELETE   USING (auth.uid() = user_id);</code></pre></p>\n\n<h3>Mistake 3: Using <code>true</code> as the Policy Condition</h3>\n\n<p><strong>What happened:</strong> The developer (or AI) created a policy with <code>USING (true)</code> to quickly unblock development. This policy allows every authenticated (and sometimes unauthenticated) user to access every row.</p>\n\n<p><strong>Why it matters:</strong> A policy with <code>USING (true)</code> is functionally equivalent to not having RLS at all. It satisfies the RLS-is-enabled checkbox without actually providing any access control.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT schemaname, tablename, policyname, permissive, cmd, qual\nFROM pg_policies\nWHERE schemaname = 'public'\n  AND qual = 'true';</code></pre>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">DROP POLICY &quot;allow all&quot; ON public.your_table;\n\n<p>CREATE POLICY &quot;owner access only&quot;   ON public.your_table FOR ALL   USING (auth.uid() = user_id)   WITH CHECK (auth.uid() = user_id);</code></pre></p>\n\n<p>If some data genuinely needs to be public (e.g., published blog posts), use a policy that checks a status column:</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;public can read published posts&quot;\n  ON public.posts FOR SELECT\n  USING (status = 'published');</code></pre>\n\n<h3>Mistake 4: Forgetting WITH CHECK on INSERT and UPDATE</h3>\n\n<p><strong>What happened:</strong> The developer created a SELECT policy with <code>USING</code> but did not add <code>WITH CHECK</code> for INSERT or UPDATE operations. This is subtle because they serve different purposes:</p>\n\n<ul>\n<li><code>USING</code> filters which <em>existing</em> rows a user can see, update, or delete.</li>\n<li><code>WITH CHECK</code> validates what data a user can <em>write</em> during inserts and updates.</li>\n</ul>\n\n<p><strong>Why it matters:</strong> Without <code>WITH CHECK</code> on INSERT, there is no validation on new rows being created. A user could insert data with a different <code>user_id</code>, effectively writing data into another user's namespace.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT tablename, cmd, policyname,\n       qual IS NOT NULL AS has_using,\n       with_check IS NOT NULL AS has_with_check\nFROM pg_policies\nWHERE schemaname = 'public'\nORDER BY tablename, cmd;</code></pre>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;users can insert own data&quot;\n  ON public.profiles FOR INSERT\n  WITH CHECK (auth.uid() = user_id);\n\n<p>CREATE POLICY &quot;users can update own profile&quot;   ON public.profiles FOR UPDATE   USING (auth.uid() = user_id)   WITH CHECK (auth.uid() = user_id);</code></pre></p>\n\n<h3>Mistake 5: Not Indexing Columns Used in Policies</h3>\n\n<p><strong>What happened:</strong> RLS policies reference columns like <code>user_id</code> or <code>organization_id</code> in their <code>USING</code> and <code>WITH CHECK</code> clauses, but those columns have no indexes.</p>\n\n<p><strong>Why it matters:</strong> Every query against an RLS-protected table evaluates the policy conditions. Without indexes, this means a full table scan for every single query. On a table with 1 million rows, your API responses go from 50ms to 5 seconds. This causes developers to disable RLS for performance reasons, which creates a security vulnerability.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT tablename, policyname, qual, with_check\nFROM pg_policies WHERE schemaname = 'public';\n\n<p>SELECT indexname, tablename, indexdef FROM pg_indexes WHERE schemaname = 'public' AND tablename = 'your_table';</code></pre></p>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">CREATE INDEX idx_user_data_user_id ON public.user_data (user_id);\nCREATE INDEX idx_documents_org_id ON public.documents (organization_id);</code></pre>\n\n<h3>The Bigger Picture</h3>\n\n<p>These five mistakes share a common root cause: RLS is a security system that looks like a database feature. Developers approach it with a make-it-work mindset rather than a make-it-secure mindset. AI code generators amplify this because they optimize for functionality -- the code compiles, the queries return data, the UI renders.</p>\n\n<p>Security is what the code <em>prevents</em>, not what it enables. Storage is another overlooked attack surface -- see <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>. And that is exactly what gets lost in the vibe coding workflow.</p>\n\n<p>RLS is just one layer of your Supabase security posture. Your storage buckets are another common weak point -- read <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a> to check that attack surface too.</p>\n\n<p>The fix is not to stop using AI-generated code. The fix is to verify the security posture of what gets generated. Check your RLS. Check your policies. Check your indexes. Or let a tool check them for you.</p>\n\n<p><strong><a href=\"/login\">Scan your Supabase project for RLS misconfigurations --></a></strong></p>",
  },
  {
    slug: "supabase-storage-buckets-probably-public",
    title: "Why Your Supabase Storage Buckets Are Probably Public",
    description:
      "Most Supabase storage buckets are accidentally public. Learn how to audit your bucket permissions, fix common misconfigurations, and lock down file access.",
    category: "Storage",
    publishedAt: "2026-03-20",
    keywords: [
      "supabase storage security",
      "supabase public bucket",
      "supabase storage policies",
    ],
    body: "<p>Let us start with a question: if someone guessed the URL to a file in your Supabase storage bucket, could they download it?</p>\n\n<p>If you are using a public bucket -- and there is a good chance you are -- the answer is yes. No authentication required. No tokens. No permissions check. Just a URL and a browser.</p>\n\n<p>This is not a bug. This is how public buckets are designed to work. This is part of a broader <a href=\"/blog/state-of-supabase-security-2026\">Supabase security problem we documented</a>. The problem is that public is the path of least resistance, and many applications are storing files that should not be public at all.</p>\n\n<p>For a broader view of Supabase security challenges, including RLS and API key exposure, see <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>.</p>\n\n<h3>Public vs. Private: What the Labels Actually Mean</h3>\n\n<p>Supabase Storage buckets come in two flavors:</p>\n\n<p><strong>Public buckets</strong> serve files at a predictable, unauthenticated URL:</p>\n\n<pre><code class=\"language-text\">https://{project-ref}.supabase.co/storage/v1/object/public/{bucket}/{path}</code></pre>\n\n<p>Anyone with this URL can download the file. There is no access check on reads. The file is effectively on the open internet.</p>\n\n<p>Upload, delete, move, and copy operations still require authentication and are controlled by RLS policies on the <code>storage.objects</code> table. But reads? Completely open.</p>\n\n<p><strong>Private buckets</strong> enforce RLS policies on <em>all</em> operations, including reads. To access a file, you either need a valid JWT (authenticated request) or a signed URL (temporary, time-limited access link).</p>\n\n<h3>Why Developers Choose Public Buckets</h3>\n\n<p>Public buckets are simpler. That is the entire reason.</p>\n\n<p>With a public bucket: no need to generate signed URLs, no need to handle URL expiration and refresh, no need to proxy requests through a backend. Image tags and file links work with plain URLs. Less code, fewer moving parts, faster development.</p>\n\n<p>With a private bucket: every file access requires either a signed URL (which expires) or an authenticated request. You need token management on the client side. More code, more complexity, more things that can break.</p>\n\n<p>When a developer (or an AI assistant) is trying to get file uploads working, the public bucket approach gets them to it-works faster. And in vibe coding, it-works is usually where development stops.</p>\n\n<h3>What Is Actually at Risk</h3>\n\n<p>The risk depends on what you are storing. Consider what typically ends up in Supabase storage buckets:</p>\n\n<ul>\n<li><strong>User profile photos:</strong> Often contain EXIF metadata with GPS coordinates, device info, and timestamps.</li>\n<li><strong>Document uploads:</strong> Resumes, contracts, identification documents, invoices.</li>\n<li><strong>Application assets:</strong> Screenshots, exports, reports that may contain sensitive data.</li>\n<li><strong>User-generated content:</strong> Posts, images, files shared within the application's context.</li>\n</ul>\n\n<p>The question is not can someone access the file but if someone accessed every file in this bucket, would that be a problem?</p>\n\n<h3>The Enumeration Problem</h3>\n\n<p>Public bucket URLs follow a predictable pattern. If an attacker knows one file URL:</p>\n\n<pre><code class=\"language-text\">https://abcdefgh.supabase.co/storage/v1/object/public/avatars/user_123.jpg</code></pre>\n\n<p>They can guess others:</p>\n\n<pre><code class=\"language-text\">https://abcdefgh.supabase.co/storage/v1/object/public/avatars/user_124.jpg\nhttps://abcdefgh.supabase.co/storage/v1/object/public/avatars/user_125.jpg</code></pre>\n\n<p>If your file naming scheme is sequential or based on predictable identifiers, enumeration is trivial. Even with UUIDs, if a file URL leaks through browser history, referrer headers, or server logs, it becomes permanently accessible.</p>\n\n<h3>How to Check Your Buckets</h3>\n\n<p><strong>Step 1: List your buckets and their visibility.</strong></p>\n\n<pre><code class=\"language-sql\">SELECT id, name, public FROM storage.buckets;</code></pre>\n\n<p>Any bucket where <code>public = true</code> serves files without authentication.</p>\n\n<p><strong>Step 2: Check what is in your public buckets.</strong></p>\n\n<pre><code class=\"language-sql\">SELECT bucket_id, name, created_at, metadata\nFROM storage.objects\nWHERE bucket_id IN (\n  SELECT id FROM storage.buckets WHERE public = true\n)\nORDER BY created_at DESC LIMIT 50;</code></pre>\n\n<p><strong>Step 3: Test access.</strong> Open a private/incognito browser window and try accessing a file URL directly. If it downloads, anyone can access it.</p>\n\n<h3>How to Fix It</h3>\n\n<h4>Option 1: Switch to a Private Bucket</h4>\n\n<p>You cannot change a bucket visibility after creation. Create a new private bucket, move files, update your application, and delete the old public bucket.</p>\n\n<pre><code class=\"language-sql\">INSERT INTO storage.buckets (id, name, public)\nVALUES ('user-uploads-private', 'user-uploads-private', false);</code></pre>\n\n<p>Then add RLS policies:</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;Users can upload own files&quot;\n  ON storage.objects FOR INSERT\n  WITH CHECK (\n    bucket_id = 'user-uploads-private'\n    AND auth.uid()::text = (storage.foldername(name))[1]\n  );\n\n<p>CREATE POLICY &quot;Users can view own files&quot;   ON storage.objects FOR SELECT   USING (     bucket_id = 'user-uploads-private'     AND auth.uid()::text = (storage.foldername(name))[1]   );</p>\n\n<p>CREATE POLICY &quot;Users can delete own files&quot;   ON storage.objects FOR DELETE   USING (     bucket_id = 'user-uploads-private'     AND auth.uid()::text = (storage.foldername(name))[1]   );</code></pre></p>\n\n<p>Note: these storage RLS policies follow the same patterns as database table policies. If you are unfamiliar with USING vs WITH CHECK, see <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a> for a detailed breakdown.</p>\n\n<h4>Option 2: Use Signed URLs</h4>\n\n<p>For private buckets, generate time-limited signed URLs on the server side:</p>\n\n<pre><code class=\"language-javascript\">const { data, error } = await supabase.storage\n  .from('user-uploads-private')\n  .createSignedUrl('path/to/file.pdf', 3600); // expires in 1 hour</code></pre>\n\n<p>Set expiration times appropriate for your use case: short (5-15 minutes) for display purposes, longer (1-24 hours) for download links.</p>\n\n<h4>Option 3: Organize with Folder-Based Policies</h4>\n\n<p>Structure your storage so that file paths include the user ID, then write RLS policies that match the folder structure.</p>\n\n<h3>When Public Buckets Are Fine</h3>\n\n<p>Not every bucket needs to be private. Public buckets are appropriate for:</p>\n\n<ul>\n<li><strong>Marketing assets:</strong> Logos, product screenshots, social media images</li>\n<li><strong>Static content:</strong> Blog post images, documentation illustrations</li>\n<li><strong>Intentionally public resources:</strong> Open datasets, public downloads</li>\n</ul>\n\n<p>The rule of thumb: if the file would be fine on a public website, a public bucket is fine. If the file belongs to a specific user or contains any personal data, use a private bucket.</p>\n\n<h3>The Vibe Coding Connection</h3>\n\n<p>This issue is amplified by AI-generated code. When you ask an AI to add file upload to your app, the generated code typically creates a bucket (often public), implements the upload function, stores the public URL in the database, and renders the file using the public URL directly.</p>\n\n<p>The result works. Files upload and display correctly. The AI has solved the stated problem. What it has not done is evaluate whether the files being uploaded warrant access control. This is the same pattern we see across the board with vibe-coded Supabase apps -- <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a> covers the full picture.</p>\n\n<h3>Action Items</h3>\n\n<ol>\n<li><strong>Audit your buckets now.</strong> Run the SQL query above. Know which buckets are public.</li>\n<li><strong>Review the contents.</strong> Determine whether public access is appropriate for the files stored in each bucket.</li>\n<li><strong>Migrate sensitive data to private buckets.</strong> Create new private buckets, move files, update your application code.</li>\n<li><strong>Use signed URLs for private file access.</strong> Set appropriate expiration times.</li>\n<li><strong>Add storage policies.</strong> Use folder-based RLS policies that scope access to the file owner.</li>\n<li><strong>Automate the check.</strong> Include storage bucket auditing in your security review process, or <a href=\"/login\">run a free scan to check your storage configuration automatically</a>.</li>\n</ol>\n\n<p>Your users trust you with their files. Make sure that trust is warranted.</p>\n\n<p><strong><a href=\"/login\">Get a free security scan of your Supabase project --></a></strong></p>",
  },
  {
    slug: "how-to-audit-supabase-rls-policies",
    title: "How to Audit Your Supabase RLS Policies (Step-by-Step Guide)",
    description:
      "A step-by-step guide to manually auditing your Supabase RLS policies. Find missing policies, overly permissive conditions, and security gaps with SQL queries you can run today.",
    category: "Guides",
    publishedAt: "2026-03-28",
    keywords: [
      "supabase rls audit",
      "check supabase security",
      "audit rls policies",
      "supabase security audit",
    ],
    body: "<p>Your Supabase application is live. Users are signing up, data is flowing, everything works. But have you actually looked at your Row Level Security policies since you wrote them?</p>\n\n<p>RLS policies are the access control layer between your data and anyone with your project's anon key. If they are misconfigured -- or missing entirely -- your database is exposed. As we documented in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>, RLS misconfiguration is the most common vulnerability pattern in Supabase applications.</p>\n\n<p>This guide walks you through a complete manual audit of your RLS policies, step by step. By the end, you will know exactly where your security gaps are and how to close them.</p>\n\n<h3>Before You Start</h3>\n\n<p>You will need access to the Supabase SQL Editor for your project or a direct connection to your Postgres database. Every query in this guide runs against system catalogs and does not modify your data or schema.</p>\n\n<p>Open the SQL Editor in your Supabase dashboard and follow along.</p>\n\n<h3>Step 1: Inventory All Public Tables</h3>\n\n<p>Start by listing every table in your public schema. These are the tables exposed through Supabase's auto-generated REST API.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename\nFROM pg_tables\nWHERE schemaname = 'public'\nORDER BY tablename;</code></pre>\n\n<p>Write down every table name. This is your audit scope. Every table on this list is accessible through the API and needs to be accounted for.</p>\n\n<h3>Step 2: Check RLS Status on Every Table</h3>\n\n<p>For each table in your inventory, verify that RLS is enabled.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename, rowsecurity\nFROM pg_tables\nWHERE schemaname = 'public'\nORDER BY tablename;</code></pre>\n\n<p><strong>What to look for:</strong> Every row should show <code>rowsecurity = true</code>. Any table with <code>false</code> is completely unprotected -- anyone with your anon key can read, insert, update, and delete every row.</p>\n\n<p><strong>Fix immediately:</strong></p>\n\n<pre><code class=\"language-sql\">ALTER TABLE public.your_table ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.your_table FORCE ROW LEVEL SECURITY;</code></pre>\n\n<p>The <code>FORCE</code> statement ensures that RLS applies even to the table owner, which prevents accidental bypasses during development.</p>\n\n<h3>Step 3: Map All Existing Policies</h3>\n\n<p>Now list every RLS policy across all your public tables.</p>\n\n<pre><code class=\"language-sql\">SELECT\n  tablename,\n  policyname,\n  permissive,\n  roles,\n  cmd,\n  qual AS using_expression,\n  with_check\nFROM pg_policies\nWHERE schemaname = 'public'\nORDER BY tablename, cmd;</code></pre>\n\n<p>This gives you the complete picture: which tables have policies, what operations they cover, and what conditions they enforce. Save this output -- it is the core of your audit.</p>\n\n<h3>Step 4: Identify Tables With No Policies</h3>\n\n<p>An RLS-enabled table with no policies blocks all access. This sounds secure, but it usually means a developer will disable RLS to get things working again. Find these tables before that happens.</p>\n\n<pre><code class=\"language-sql\">SELECT t.tablename\nFROM pg_tables t\nLEFT JOIN pg_policies p\n  ON t.tablename = p.tablename AND t.schemaname = p.schemaname\nWHERE t.schemaname = 'public'\n  AND t.rowsecurity = true\nGROUP BY t.tablename\nHAVING COUNT(p.policyname) = 0;</code></pre>\n\n<p>Every table in this result needs policies added. Refer to <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a> for common policy patterns that match typical use cases.</p>\n\n<h3>Step 5: Check Operation Coverage</h3>\n\n<p>A table might have a SELECT policy but lack INSERT, UPDATE, or DELETE policies. This creates gaps where users can read data but write operations are either blocked or uncontrolled.</p>\n\n<p>For each table, check which operations are covered:</p>\n\n<pre><code class=\"language-sql\">SELECT\n  tablename,\n  ARRAY_AGG(DISTINCT cmd) AS covered_operations\nFROM pg_policies\nWHERE schemaname = 'public'\nGROUP BY tablename\nORDER BY tablename;</code></pre>\n\n<p>Compare the covered operations against what your application actually does. If your app performs INSERT and UPDATE operations on a table but only a SELECT policy exists, those write operations are either blocked by RLS or uncontrolled.</p>\n\n<p>Every operation your application performs on a table should have a corresponding policy.</p>\n\n<h3>Step 6: Find Overly Permissive Policies</h3>\n\n<p>The most dangerous policies are the ones that technically exist but provide no real access control. The classic example is <code>USING (true)</code>, which allows every user to access every row.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename, policyname, cmd, qual, with_check\nFROM pg_policies\nWHERE schemaname = 'public'\n  AND (qual = 'true' OR with_check = 'true');</code></pre>\n\n<p>Any result here is a red flag. A policy with <code>USING (true)</code> is functionally equivalent to having RLS disabled. Replace these with policies that actually check ownership or permissions. We detail the fix patterns in <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<h3>Step 7: Verify WITH CHECK Clauses</h3>\n\n<p>INSERT and UPDATE operations need <code>WITH CHECK</code> clauses to validate what data users can write. Without them, a user might insert rows with another user's ID or update rows to change ownership.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename, policyname, cmd,\n  qual IS NOT NULL AS has_using,\n  with_check IS NOT NULL AS has_with_check\nFROM pg_policies\nWHERE schemaname = 'public'\n  AND cmd IN ('INSERT', 'UPDATE', 'ALL')\nORDER BY tablename;</code></pre>\n\n<p><strong>What to look for:</strong> INSERT policies should have <code>WITH CHECK</code>. UPDATE policies should have both <code>USING</code> (which existing rows can be updated) and <code>WITH CHECK</code> (what the new values can be). If <code>has_with_check</code> is false on any INSERT or UPDATE policy, you have a validation gap.</p>\n\n<h3>Step 8: Check for Missing Indexes</h3>\n\n<p>This step is about performance, but performance problems cause security problems. If RLS policies make queries slow, developers disable RLS. Check that every column referenced in your policies has an index.</p>\n\n<pre><code class=\"language-sql\">SELECT indexname, tablename, indexdef\nFROM pg_indexes\nWHERE schemaname = 'public'\nORDER BY tablename;</code></pre>\n\n<p>Cross-reference this with the <code>qual</code> and <code>with_check</code> columns from Step 3. If your policy references <code>user_id</code> but there is no index on that column, add one:</p>\n\n<pre><code class=\"language-sql\">CREATE INDEX idx_your_table_user_id ON public.your_table (user_id);</code></pre>\n\n<h3>Step 9: Test Policies with Role Switching</h3>\n\n<p>The final step in a manual audit is testing. Switch to the <code>anon</code> and <code>authenticated</code> roles in the SQL Editor and verify that policies behave as expected.</p>\n\n<pre><code class=\"language-sql\">-- Test as anon user (no auth)\nSET ROLE anon;\nSELECT * FROM public.your_table;\nRESET ROLE;\n\n-- Test as authenticated user\nSET ROLE authenticated;\nSET request.jwt.claims = '{&quot;sub&quot;: &quot;test-user-id&quot;}';\nSELECT * FROM public.your_table;\nRESET ROLE;</code></pre>\n\n<p>If you can see rows that should be restricted, your policies have gaps.</p>\n\n<h3>Putting It All Together</h3>\n\n<p>A complete manual RLS audit covers nine steps: inventory tables, verify RLS is enabled, map all policies, find tables with no policies, check operation coverage, identify permissive conditions, verify WITH CHECK clauses, check indexes, and test with role switching.</p>\n\n<p>This process takes 30 to 60 minutes for a small project. For larger applications with dozens of tables, it can take half a day or more. And it needs to be repeated every time your schema changes -- every new table, every new migration, every feature shipped by a teammate or generated by an AI assistant.</p>\n\n<p>That is the fundamental limitation of manual auditing. It is thorough but expensive, and it does not scale with the speed of modern development.</p>\n\n<h3>The Automated Alternative</h3>\n\n<p>SupaScanner performs this entire audit automatically in under 40 seconds. It checks every table for RLS status, analyzes all policies for completeness and overly permissive conditions, verifies operation coverage, inspects storage buckets, and generates copy-paste SQL fixes for every issue found.</p>\n\n<p>You get an A through F security grade, a prioritized list of vulnerabilities, and the exact SQL to fix each one. No manual queries, no cross-referencing system catalogs, no wondering if you missed something.</p>\n\n<p>Manual auditing is valuable for understanding your security posture in depth. Automated scanning is how you keep it secure as your application evolves.</p>\n\n<p><strong><a href=\"/login\">Run a free security audit of your Supabase project --></a></strong></p>",
  },
  {
    slug: "supabase-security-checklist-production",
    title:
      "Supabase Security Checklist: 15 Things to Check Before Going to Production",
    description:
      "The complete Supabase production security checklist. 15 things to verify before launch -- from RLS policies and storage buckets to auth configuration and API key management.",
    category: "Guides",
    publishedAt: "2026-03-29",
    keywords: [
      "supabase production checklist",
      "supabase security checklist",
      "supabase production ready",
      "supabase go live checklist",
    ],
    body: "<p>You have built your application on Supabase. The features work, the UI is polished, and you are ready to launch. But before you point real users at your project, there is one question worth asking: is it actually secure?</p>\n\n<p>Supabase gives you a powerful Postgres database with auto-generated APIs, built-in authentication, and file storage. It also gives you a lot of ways to misconfigure those features. As we documented in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>, the most common vulnerabilities are not exotic attacks -- they are configuration mistakes that expose data through Supabase's own APIs.</p>\n\n<p>This checklist covers the 15 things you should verify before going to production. Work through it sequentially -- the items are ordered by severity and each one builds on the previous.</p>\n\n<h3>1. Enable RLS on Every Public Table</h3>\n\n<p>This is the most critical security setting in your entire Supabase project. Every table in the <code>public</code> schema is exposed through the auto-generated REST API. Without RLS, anyone with your project URL and anon key has unrestricted access.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename, rowsecurity\nFROM pg_tables\nWHERE schemaname = 'public';</code></pre>\n\n<p>Every table must show <code>rowsecurity = true</code>. No exceptions. If you find any table with RLS disabled, fix it immediately:</p>\n\n<pre><code class=\"language-sql\">ALTER TABLE public.your_table ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.your_table FORCE ROW LEVEL SECURITY;</code></pre>\n\n<h3>2. Verify Policies Exist for All RLS-Enabled Tables</h3>\n\n<p>RLS enabled with zero policies means the table blocks all access. This is technically secure, but it often leads developers to disable RLS in frustration. Verify that every table with RLS has appropriate policies:</p>\n\n<pre><code class=\"language-sql\">SELECT t.tablename, COUNT(p.policyname) AS policy_count\nFROM pg_tables t\nLEFT JOIN pg_policies p ON t.tablename = p.tablename AND t.schemaname = p.schemaname\nWHERE t.schemaname = 'public' AND t.rowsecurity = true\nGROUP BY t.tablename\nORDER BY policy_count ASC;</code></pre>\n\n<p>Any table with <code>policy_count = 0</code> needs policies before launch. See our step-by-step <a href=\"/blog/how-to-audit-supabase-rls-policies\">RLS audit guide</a> for detailed policy patterns.</p>\n\n<h3>3. Remove Overly Permissive Policies</h3>\n\n<p>A policy with <code>USING (true)</code> allows every user to access every row. This is functionally equivalent to not having RLS.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename, policyname, cmd, qual\nFROM pg_policies\nWHERE schemaname = 'public' AND qual = 'true';</code></pre>\n\n<p>Replace every result with a policy that checks actual ownership or permissions. See <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a> for fix patterns.</p>\n\n<h3>4. Add WITH CHECK on INSERT and UPDATE Policies</h3>\n\n<p>Without <code>WITH CHECK</code>, there is no validation on what data users write. A user could insert rows with another user's ID.</p>\n\n<pre><code class=\"language-sql\">SELECT tablename, policyname, cmd, with_check\nFROM pg_policies\nWHERE schemaname = 'public'\n  AND cmd IN ('INSERT', 'UPDATE', 'ALL')\n  AND with_check IS NULL;</code></pre>\n\n<p>Every INSERT policy needs <code>WITH CHECK</code>. Every UPDATE policy needs both <code>USING</code> and <code>WITH CHECK</code>.</p>\n\n<h3>5. Index Columns Referenced in RLS Policies</h3>\n\n<p>RLS policy conditions run on every query. Without indexes on the referenced columns, you get full table scans that degrade performance and tempt developers to disable RLS.</p>\n\n<p>Cross-reference your policy conditions with your indexes. Every <code>user_id</code>, <code>organization_id</code>, or similar column in a policy <code>USING</code> clause should have an index.</p>\n\n<pre><code class=\"language-sql\">CREATE INDEX IF NOT EXISTS idx_your_table_user_id ON public.your_table (user_id);</code></pre>\n\n<p><strong>Items 1 through 5 cover your RLS posture -- the most common source of Supabase vulnerabilities.</strong> <a href=\"/login\">SupaScanner checks all five automatically</a> and generates the exact SQL fixes.</p>\n\n<h3>6. Audit Storage Bucket Visibility</h3>\n\n<p>Public storage buckets serve files to anyone with the URL. No authentication required.</p>\n\n<pre><code class=\"language-sql\">SELECT id, name, public FROM storage.buckets;</code></pre>\n\n<p>Any bucket containing user data, uploads, or sensitive files should be private. Read <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a> for a detailed guide on migrating to private buckets with signed URLs.</p>\n\n<h3>7. Add RLS Policies to storage.objects</h3>\n\n<p>Even with private buckets, the <code>storage.objects</code> table needs RLS policies to control who can upload, read, and delete files. Without policies, storage operations default to deny -- but adding an overly broad policy to make uploads work is a common mistake.</p>\n\n<pre><code class=\"language-sql\">SELECT policyname, cmd, qual, with_check\nFROM pg_policies\nWHERE tablename = 'objects' AND schemaname = 'storage';</code></pre>\n\n<p>Verify that storage policies scope access to the file owner, typically using folder-based patterns with <code>auth.uid()</code>.</p>\n\n<h3>8. Confirm service_role Key Is Not in Frontend Code</h3>\n\n<p>The <code>service_role</code> key bypasses all RLS. If it appears in your frontend code, your entire database is accessible to every user.</p>\n\n<p>Search your frontend codebase for your service_role key. Check environment variables, config files, and bundled JavaScript. If you find it anywhere that ships to the browser, you have a critical vulnerability.</p>\n\n<p>Your frontend should only ever use the <code>anon</code> key. All service_role operations belong on the server.</p>\n\n<h3>9. Enable Email Confirmation for Auth</h3>\n\n<p>Without email confirmation, anyone can sign up with any email address -- including addresses they do not own. This makes it trivial to impersonate users or create fake accounts at scale.</p>\n\n<p>In the Supabase dashboard, go to Authentication, then Settings, and verify that <strong>Enable email confirmations</strong> is turned on. For production applications, this is non-negotiable.</p>\n\n<h3>10. Review OAuth Provider Configuration</h3>\n\n<p>If you use third-party OAuth providers (Google, GitHub, etc.), verify that each provider is configured with the correct redirect URLs. Misconfigured redirect URLs can enable token theft through open redirect attacks.</p>\n\n<p>Also verify that you are only enabling OAuth providers your application actually uses. Each enabled provider is an additional authentication surface that needs to be maintained.</p>\n\n<h3>11. Restrict Signup to Allowed Email Domains</h3>\n\n<p>If your application is intended for a specific organization or user base, restrict signups to allowed email domains. This prevents unauthorized accounts from being created.</p>\n\n<p>You can implement this with a database function and trigger that validates the email domain during user creation, or by using Supabase's built-in email domain restrictions if your plan supports them.</p>\n\n<h3>12. Disable the Data API If Unused</h3>\n\n<p>Supabase auto-generates REST and GraphQL APIs for every table in the <code>public</code> schema. If your application only accesses the database through server-side code or Edge Functions, you do not need these APIs exposed.</p>\n\n<p>Disabling the Data API removes an entire attack surface. You can do this in the Supabase dashboard under API Settings. If your application still works after disabling it, leave it off.</p>\n\n<h3>13. Review Database Roles and Privileges</h3>\n\n<p>Supabase creates several database roles with different privilege levels. Review what each role can do and verify that no custom roles have been granted excessive permissions.</p>\n\n<pre><code class=\"language-sql\">SELECT grantee, table_schema, table_name, privilege_type\nFROM information_schema.table_privileges\nWHERE table_schema = 'public'\nORDER BY grantee, table_name;</code></pre>\n\n<p>The <code>anon</code> and <code>authenticated</code> roles should only have the minimum privileges required for your application to function.</p>\n\n<h3>14. Enable and Review Supabase Security Advisor</h3>\n\n<p>Supabase includes a built-in Security Advisor in the project dashboard. It flags common misconfigurations including weak RLS, exposed credentials, and risky database settings.</p>\n\n<p>Run the Security Advisor and address every finding. It is not exhaustive -- it focuses on the most common issues -- but it catches problems that are easy to overlook.</p>\n\n<h3>15. Set Up Continuous Security Monitoring</h3>\n\n<p>A security checklist is a point-in-time snapshot. Your application changes with every deployment: new tables, modified policies, updated auth settings. What is secure today may not be secure after next week's feature push.</p>\n\n<p>Continuous monitoring means security scanning runs automatically as part of your development workflow, not as a one-time pre-launch activity. This is especially important for teams using vibe coding or AI-assisted development, where schema changes can happen rapidly without manual security review.</p>\n\n<p><a href=\"/login\">SupaScanner provides automated security scanning</a> that checks all 15 items on this list. Run it before launch, after every significant change, and on a regular schedule to catch configuration drift.</p>\n\n<h3>The Checklist Summary</h3>\n\n<p>Before going to production, verify:</p>\n\n<ol>\n<li>RLS enabled on every public table</li>\n<li>Policies exist for all RLS-enabled tables</li>\n<li>No <code>USING (true)</code> or overly permissive policies</li>\n<li><code>WITH CHECK</code> on all INSERT and UPDATE policies</li>\n<li>Indexes on all columns referenced in policies</li>\n<li>Storage buckets set to private where appropriate</li>\n<li>RLS policies on <code>storage.objects</code></li>\n<li><code>service_role</code> key not in frontend code</li>\n<li>Email confirmation enabled</li>\n<li>OAuth providers correctly configured</li>\n<li>Signup restricted to allowed domains if applicable</li>\n<li>Data API disabled if unused</li>\n<li>Database roles reviewed for minimal privileges</li>\n<li>Supabase Security Advisor findings addressed</li>\n<li>Continuous security monitoring in place</li>\n</ol>\n\n<p>Each item on this list has caused real data exposures in real Supabase applications. Do not ship without checking all 15.</p>\n\n<p><strong><a href=\"/login\">Get your Supabase security grade in 40 seconds --></a></strong></p>",
  },
  {
    slug: "vibe-coding-automated-security-scanning",
    title: "Why Vibe Coding Needs Automated Security Scanning",
    description:
      "AI-generated code ships fast but skips security. Learn why automated security scanning is the missing safety net for vibe-coded Supabase applications.",
    category: "Vibe Coding",
    publishedAt: "2026-03-30",
    keywords: [
      "vibe coding security",
      "ai generated code security",
      "automated security scanning",
      "vibe coding risks",
    ],
    body: "<p>Vibe coding changed how software gets built. You describe what you want, an AI writes the code, and you ship it. The feedback loop that used to take hours -- plan, write, debug, test -- now takes minutes. Entire applications go from idea to deployed in a single afternoon.</p>\n\n<p>This is genuinely transformative. It is also creating a security crisis that most developers have not yet felt.</p>\n\n<h3>The Vibe Coding Tradeoff</h3>\n\n<p>The core proposition of vibe coding is speed. And it delivers. A developer can prompt their way through features, UI components, database schemas, and API integrations at a pace that was not possible even two years ago.</p>\n\n<p>But speed and security have always been in tension. Traditional development had natural checkpoints: code reviews, design documents, deployment checklists. These processes were slow, but they created opportunities to catch mistakes before they reached production.</p>\n\n<p>Vibe coding removes those checkpoints. The developer goes from prompt to production with nothing in between except &quot;it works&quot; -- and &quot;it works&quot; has never been the same as &quot;it is secure.&quot;</p>\n\n<h3>What AI Gets Wrong About Security</h3>\n\n<p>AI code generators are optimized for functionality. When you ask an AI to add a comments table to your Supabase project, it will create the table, write the schema, generate the client queries, and wire up the UI. What it typically will not do:</p>\n\n<ul>\n<li>Enable RLS with appropriate per-user policies</li>\n<li>Add <code>WITH CHECK</code> clauses to prevent users from writing data as other users</li>\n<li>Verify that storage buckets are private for sensitive uploads</li>\n<li>Check whether the <code>service_role</code> key is being used where the <code>anon</code> key should be</li>\n<li>Add indexes on columns used in security policies</li>\n</ul>\n\n<p>This is not because AI models are bad at security. It is because security is about what the code <em>prevents</em>, and AI is optimized for what the code <em>enables</em>. The model succeeds when queries return data and the UI renders. It has no way to verify that unauthorized queries do not also return data.</p>\n\n<p>Research supports this: Veracode found that approximately half of AI-generated code contains known vulnerabilities. The problem is systemic, not occasional.</p>\n\n<h3>The Supabase Amplifier</h3>\n\n<p>This issue is particularly acute for Supabase applications. Supabase's auto-generated APIs mean that every table in the <code>public</code> schema is directly accessible from the frontend. Unlike traditional backend architectures where a server-side layer mediates database access, Supabase clients talk directly to Postgres through the API.</p>\n\n<p>That design is what makes Supabase fast to build with. It is also what makes misconfiguration so dangerous. A missing RLS policy does not just mean a theoretical vulnerability -- it means anyone with a browser can query your database.</p>\n\n<p>We have documented the specific patterns in detail: <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a> covers the landscape, <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a> covers the most common RLS issues, and <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a> covers the storage attack surface. These are not edge cases. They are the default output of AI-generated Supabase code.</p>\n\n<h3>Why Manual Review Does Not Scale</h3>\n\n<p>The obvious answer is &quot;just review the code.&quot; But this misunderstands the vibe coding workflow.</p>\n\n<p>A developer using AI-assisted tools might generate and accept 50 or 100 code changes in a single session. Each change might be small -- a new component, a query, a migration. Reviewing each one for security implications requires expertise that most developers do not have and time that the workflow does not allow.</p>\n\n<p>This is not laziness. It is arithmetic. If a security review adds even 5 minutes per change, a 50-change session gains over 4 hours of review time. The entire point of vibe coding was to move faster than that.</p>\n\n<p>Manual review also requires knowing what to look for. You need to understand RLS policy semantics, the difference between <code>USING</code> and <code>WITH CHECK</code>, storage bucket visibility implications, and API key privilege levels. Most developers using vibe coding are building their first or second Supabase project. They do not have the pattern-recognition skills that come from years of Postgres security experience.</p>\n\n<h3>The Case for Automated Scanning</h3>\n\n<p>If manual review does not scale and AI does not generate secure code by default, the remaining option is automated scanning that runs independently of the developer.</p>\n\n<p>Automated security scanning for vibe-coded applications should do three things:</p>\n\n<p><strong>1. Check what the AI actually deployed, not what it was supposed to deploy.</strong> Scanning should evaluate the live database configuration -- actual RLS status, actual policies, actual bucket settings -- not the source code or migration files. The truth is what Postgres says, not what the migration intended.</p>\n\n<p><strong>2. Produce actionable fixes, not just warnings.</strong> Telling a vibe coder that their RLS policies are insufficient is not helpful if they do not know how to write better ones. Scanning should generate the exact SQL to fix each issue -- copy, paste, run.</p>\n\n<p><strong>3. Run fast enough to fit the vibe coding workflow.</strong> A 30-minute security audit is a non-starter when the entire build session takes 45 minutes. Scanning needs to complete in seconds or minutes, not hours.</p>\n\n<h3>What This Looks Like in Practice</h3>\n\n<p>Consider a typical vibe coding session: you build a SaaS app with user profiles, a content table, file uploads, and authentication. The AI generates everything in about 20 minutes. You deploy to Supabase and start testing.</p>\n\n<p>Before sending the link to anyone, you run a security scan. In under a minute, you get back:</p>\n\n<ul>\n<li>The <code>profiles</code> table has RLS enabled but uses <code>USING (true)</code> -- effectively open. Here is the replacement policy.</li>\n<li>The <code>content</code> table has no DELETE policy. Users cannot delete their own content, but if you add one later without proper scoping it will be uncontrolled. Here is the recommended SQL.</li>\n<li>The <code>uploads</code> bucket is public. User files are accessible without authentication. Here is how to migrate to a private bucket.</li>\n<li>Storage RLS policies are missing <code>WITH CHECK</code> on INSERT. Users could upload files to other users' folders. Here is the fix.</li>\n</ul>\n\n<p>You paste the SQL fixes, run them, and scan again. Clean results. Total time: 5 minutes. You just caught four vulnerabilities that the AI introduced and that manual review might have missed.</p>\n\n<p>That is the model. Build fast with AI, verify automatically, fix with copy-paste SQL, and ship with confidence.</p>\n\n<h3>Security as a Feature of the Workflow</h3>\n\n<p>The vibe coding revolution is not going to slow down. Developers are not going to stop using AI to generate code. And AI is not going to start generating perfectly secure code on its own -- not anytime soon.</p>\n\n<p>The answer is not to choose between speed and security. It is to make security an automated step in the fast workflow rather than a manual gate that slows it down.</p>\n\n<p>If you are building on Supabase with AI-assisted tools -- and in 2026, most developers are -- automated security scanning is not optional. It is the safety net that makes vibe coding viable for production applications.</p>\n\n<p><strong><a href=\"/login\">Scan your vibe-coded Supabase project for free --></a></strong></p>",
  },
  {
    slug: "why-we-built-supabase-security-scanner",
    title: "Why We Built a Supabase Security Scanner",
    description:
      "The origin story of SupaScanner: how we discovered that most vibe-coded Supabase apps ship with critical security misconfigurations, and why we built an automated scanner to fix it.",
    category: "Security Basics",
    publishedAt: "2026-04-01",
    keywords: [
      "supabase security scanner",
      "supabase RLS checker",
      "vibe coding security",
      "supabase security tool",
      "supabase security audit tool",
    ],
    body: "<p>Today we are launching SupaScanner, a free security scanner for Supabase projects. This is the story of why it exists.</p>\n\n<p>It started with a side project. Late last year, we were building a SaaS application on Supabase. The stack was standard: Next.js frontend, Supabase for auth, database, and storage, deployed on Vercel. We used AI-assisted tools for most of the code generation. The app worked. Users could sign up, create content, upload files. Everything functioned exactly as intended.</p>\n\n<p>Then we ran a manual security review.</p>\n\n<h3>The Discovery</h3>\n\n<p>The review took about two hours. What we found was not subtle:</p>\n\n<ul>\n<li>Three tables had RLS disabled entirely. Every row was accessible to any authenticated user -- and in one case, to unauthenticated requests using just the anon key.</li>\n<li>Two tables had RLS enabled but used <code>USING (true)</code> as the policy condition, which is functionally the same as having no RLS at all.</li>\n<li>The storage bucket for user uploads was public. Every file uploaded by every user was accessible via a predictable URL. No authentication required.</li>\n<li>INSERT policies were missing <code>WITH CHECK</code> clauses. Users could write data as other users by modifying the <code>user_id</code> field in their request.</li>\n</ul>\n\n<p>None of this showed up during development. The application worked correctly from the user's perspective. The AI-generated code compiled, the queries returned data, the UI rendered. The security posture was invisible unless you went looking for it.</p>\n\n<p>We fixed everything. But the experience left a question: if our project had these issues, how many others did too?</p>\n\n<h3>The Pattern</h3>\n\n<p>We started looking at open-source Supabase projects on GitHub. We read through migration files, client configurations, and RLS policies in dozens of repositories. The pattern was consistent:</p>\n\n<p><strong>Most Supabase applications built with AI assistance share the same security misconfigurations.</strong></p>\n\n<p>The specific issues varied, but they clustered around a small number of categories:</p>\n\n<ol>\n<li><strong>Missing or disabled RLS.</strong> Tables exposed through the auto-generated API with no row-level access control. We documented this pattern in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>.</li>\n<li><strong>Permissive RLS policies.</strong> Policies that technically exist but use <code>USING (true)</code> or other conditions that grant universal access. We cataloged the five most common variants in <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</li>\n<li><strong>Public storage buckets.</strong> User uploads, documents, and profile photos accessible to anyone who can guess or enumerate the URL. We wrote a detailed breakdown in <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>.</li>\n<li><strong>Incomplete policy coverage.</strong> SELECT policies without corresponding INSERT, UPDATE, or DELETE policies. Or write policies missing <code>WITH CHECK</code> validation.</li>\n<li><strong>Missing indexes on policy columns.</strong> RLS policies that reference <code>user_id</code> or <code>organization_id</code> without indexes, causing full table scans that degrade performance and incentivize developers to disable RLS.</li>\n</ol>\n\n<p>These are not exotic attack vectors. They are configuration mistakes that happen because Supabase's security model is powerful but requires active setup, and the tools developers use to generate code do not perform that setup.</p>\n\n<h3>Why This Happens</h3>\n\n<p>The root cause is a mismatch between how Supabase works and how AI-generated code treats it.</p>\n\n<p>Supabase exposes your Postgres database directly to the frontend through auto-generated REST and GraphQL APIs. This is by design -- it is what makes Supabase fast to build with. But it means that every table in the <code>public</code> schema is one API call away from being queried by anyone with your project URL and anon key. Both of those values are embedded in your frontend JavaScript. They are not secrets.</p>\n\n<p>The only thing standing between your data and the public internet is your RLS configuration. If RLS is disabled, misconfigured, or incomplete, your data is exposed. There is no server-side middleware to fall back on. There is no API gateway filtering requests. It is RLS or nothing.</p>\n\n<p>AI code generators do not understand this architecture. When you ask an AI to create a users table and build a profile page, it generates the schema, writes the queries, and wires up the components. The application works. But the AI optimizes for functionality -- making queries return data -- not for security -- preventing unauthorized queries from returning data.</p>\n\n<p>The result is that the default output of AI-assisted Supabase development is a working application with an insecure database configuration. Not sometimes. Nearly every time.</p>\n\n<h3>What We Built</h3>\n\n<p>SupaScanner connects to your Supabase project using read-only database credentials and runs a comprehensive security audit in under 40 seconds. Here is what it checks:</p>\n\n<p><strong>RLS Status.</strong> Every table in the <code>public</code> schema is checked for Row Level Security. Tables with RLS disabled are flagged as critical findings with the exact SQL to enable and force RLS.</p>\n\n<p><strong>Policy Coverage.</strong> For each RLS-enabled table, the scanner verifies that policies exist for every operation type the table supports. It identifies tables with RLS enabled but zero policies (effectively inaccessible), tables with only SELECT policies but no write-operation policies, and tables where INSERT or UPDATE policies are missing <code>WITH CHECK</code> clauses.</p>\n\n<p><strong>Policy Permissiveness.</strong> Policies using <code>USING (true)</code>, <code>WITH CHECK (true)</code>, or other universally permissive conditions are flagged. The scanner generates replacement policies scoped to the authenticated user.</p>\n\n<p><strong>Storage Bucket Visibility.</strong> Every storage bucket is checked for its public/private setting. Public buckets containing user-uploaded content are flagged with instructions for migrating to private buckets with signed URL access.</p>\n\n<p><strong>Storage Policies.</strong> The <code>storage.objects</code> table's RLS policies are audited for proper scoping. Missing or overly broad storage policies are flagged with folder-based policy templates.</p>\n\n<p><strong>Auth Configuration.</strong> The scanner checks whether email confirmation is enabled, reviews MFA settings, and validates that the authentication configuration follows security best practices.</p>\n\n<p><strong>Performance Indexing.</strong> Columns referenced in RLS policy conditions are cross-referenced with existing indexes. Missing indexes are flagged because they cause performance degradation that leads developers to disable RLS -- turning a performance issue into a security vulnerability.</p>\n\n<p>Every finding includes an A-through-F severity grade and the exact SQL to fix it. No vague recommendations. No &quot;consider reviewing your policies.&quot; Copy the SQL, paste it into the SQL Editor, run it. Done.</p>\n\n<h3>The Technical Approach</h3>\n\n<p>We designed the scanner around three principles:</p>\n\n<p><strong>Check the live database, not the source code.</strong> Migration files can lie. The developer might have modified policies through the Supabase dashboard after running migrations. The SQL Editor might have been used to disable RLS during debugging. The only source of truth is what Postgres reports, so we query the system catalogs directly: <code>pg_tables</code>, <code>pg_policies</code>, <code>pg_indexes</code>, <code>storage.buckets</code>, and <code>information_schema</code>.</p>\n\n<p><strong>Generate actionable SQL, not advisory text.</strong> The target user is a developer who may be building their first Supabase project. They do not need a lecture on RLS semantics. They need the exact <code>CREATE POLICY</code> statement with the right <code>USING</code> and <code>WITH CHECK</code> clauses for their specific tables and columns. The scanner generates these by analyzing the table schema, identifying likely ownership columns (<code>user_id</code>, <code>owner_id</code>, <code>created_by</code>), and producing policies that match the table's structure.</p>\n\n<p><strong>Finish fast.</strong> A security scan that takes 10 minutes is a scan that does not get run. SupaScanner completes a full audit in under 40 seconds. The scan fits naturally into the development workflow: build, deploy, scan, fix, ship. No context switching. No waiting.</p>\n\n<h3>Free Tier and Pricing</h3>\n\n<p>SupaScanner is free to start. Every account gets free scans so you can audit your project and see exactly what needs fixing before you pay anything.</p>\n\n<p>We chose this model deliberately. The security problems we are solving affect developers who are early in their Supabase journey -- often building their first production application. Charging upfront would mean the developers who need the tool most are the least likely to use it.</p>\n\n<p>Paid plans add continuous monitoring, team dashboards, and scan history. But the core value -- finding and fixing your security misconfigurations -- is available on the free tier. If your project has critical vulnerabilities, you should know about them regardless of your budget.</p>\n\n<h3>What Happens Next</h3>\n\n<p>Today is day one. We have been testing SupaScanner against our own projects and with early users for the past several weeks. The scanner has caught real vulnerabilities in real applications -- the same patterns we found in our own project and across the open-source ecosystem.</p>\n\n<p>Our roadmap includes CI/CD integration so scans run automatically on every deployment, GitHub Action support for scanning as part of pull request checks, and expanded checks covering Edge Function security, realtime channel permissions, and database function privileges.</p>\n\n<p>But the immediate value is simple: connect your Supabase project, run a scan, and know -- in under 40 seconds -- whether your data is actually protected.</p>\n\n<p>If you are building on Supabase, especially with AI-assisted tools, you should run this scan. Not because we are selling you something, but because the misconfigurations we check for are the ones that expose user data. And they are present in the majority of Supabase applications we have examined.</p>\n\n<p>The Supabase security model is strong. The default configuration of most Supabase applications is not. SupaScanner closes that gap.</p>\n\n<p>If you want to go deeper on the specific vulnerabilities SupaScanner catches, start with our <a href=\"/blog/supabase-security-checklist-production\">production security checklist</a> or read about <a href=\"/blog/vibe-coding-automated-security-scanning\">why vibe coding needs automated scanning</a>.</p>\n\n<p><strong><a href=\"/login\">Scan your Supabase project for free at supabase-scanner.vercel.app --></a></strong></p>",
  },
  {
    slug: "how-to-secure-your-supabase-app",
    title: "How to Secure Your Supabase App: The Complete Guide",
    description:
      "The definitive guide to Supabase security. Learn the 10 most common security mistakes, how to audit your RLS policies, lock down authentication, manage API keys safely, and protect your data from exposure.",
    category: "Guides",
    publishedAt: "2026-03-30",
    keywords: [
      "supabase security",
      "supabase RLS best practices",
      "secure supabase app",
      "supabase security guide",
      "supabase security best practices",
      "supabase row level security",
      "supabase api key security",
      "supabase auth best practices",
    ],
    body: "<p>Supabase makes building applications fast. It gives you a Postgres database with instant REST and GraphQL APIs, built-in authentication, file storage, and edge functions. You can go from zero to a deployed application in an afternoon.</p>\n\n<p>But Supabase also makes building <em>insecure</em> applications fast. The same auto-generated APIs that accelerate development expose every table in your public schema to anyone who knows your project URL. The same authentication system that takes minutes to set up can be misconfigured in ways that let attackers create accounts with stolen email addresses. The same storage service that handles file uploads seamlessly can serve every user's private files to the public internet.</p>\n\n<p>This guide is the single comprehensive resource for securing your Supabase application. It covers the 10 most common security mistakes, walks through a complete RLS audit, explains authentication and authorization best practices, and addresses API key management. Whether you are preparing for your first production launch or hardening an application that is already live, this is where you start.</p>\n\n<h2>Why Supabase Security Matters</h2>\n\n<p>Supabase's architecture is fundamentally different from traditional backend frameworks. In a typical server-rendered application, your database sits behind application code that validates requests, checks permissions, and filters data before anything reaches the client. In Supabase, the client talks directly to Postgres through auto-generated APIs.</p>\n\n<p>This is a feature, not a flaw. It is what makes Supabase so productive. But it shifts the security boundary from your application code to your database configuration. If your RLS policies are wrong, there is no middleware to catch the mistake. If your storage buckets are public, there is no server-side check preventing access. The database configuration <em>is</em> your security layer.</p>\n\n<p>This matters more now than ever. As we documented in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>, the combination of accelerating Supabase adoption, mainstream vibe coding, and attacker awareness of common Supabase misconfigurations has made database security a critical concern. The Moltbook incident -- where a vibe-coded social networking site exposed 1.5 million authentication tokens -- demonstrated what happens when these patterns reach production at scale.</p>\n\n<h2>The 10 Most Common Supabase Security Mistakes</h2>\n\n<p>After analyzing dozens of Supabase applications, open-source repositories, and scanner results, these are the 10 mistakes that appear most frequently. They are ordered by severity and prevalence.</p>\n\n<h3>1. RLS Disabled on Public Tables</h3>\n\n<p>The most critical mistake. When Row Level Security is disabled on a table in the <code>public</code> schema, every row is accessible to anyone with your project URL and <code>anon</code> key -- both of which are embedded in your frontend JavaScript and trivially extractable.</p>\n\n<p>Supabase now enables RLS by default on new tables (as of late 2025), but projects created before this change, tables created via raw SQL, or tables where RLS was explicitly disabled during development may still be exposed.</p>\n\n<pre><code class=\"language-sql\">-- Check which tables have RLS disabled\nSELECT tablename, rowsecurity\nFROM pg_tables\nWHERE schemaname = 'public' AND rowsecurity = false;</code></pre>\n\n<p><strong>Fix:</strong> Enable and force RLS on every public table. No exceptions.</p>\n\n<pre><code class=\"language-sql\">ALTER TABLE public.your_table ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.your_table FORCE ROW LEVEL SECURITY;</code></pre>\n\n<h3>2. Overly Permissive RLS Policies</h3>\n\n<p>A policy with <code>USING (true)</code> allows every user to access every row. This is functionally identical to having RLS disabled. Developers often write these during prototyping and forget to replace them before deploying.</p>\n\n<pre><code class=\"language-sql\">-- Find permissive policies\nSELECT tablename, policyname, cmd, qual\nFROM pg_policies\nWHERE schemaname = 'public' AND qual = 'true';</code></pre>\n\n<p><strong>Fix:</strong> Replace with policies that check actual ownership. For user-owned data, scope to <code>auth.uid()</code>.</p>\n\n<pre><code class=\"language-sql\">DROP POLICY \"allow all\" ON public.your_table;\nCREATE POLICY \"owner access\" ON public.your_table\n  FOR ALL USING (auth.uid() = user_id)\n  WITH CHECK (auth.uid() = user_id);</code></pre>\n\n<p>We catalog these patterns in detail in <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<h3>3. Missing WITH CHECK on Write Operations</h3>\n\n<p><code>USING</code> controls which rows a user can read or modify. <code>WITH CHECK</code> validates what data a user can write. Without <code>WITH CHECK</code> on INSERT and UPDATE policies, users can write data with another user's ID, effectively injecting rows into someone else's data.</p>\n\n<pre><code class=\"language-sql\">-- Find write policies missing WITH CHECK\nSELECT tablename, policyname, cmd\nFROM pg_policies\nWHERE schemaname = 'public'\n  AND cmd IN ('INSERT', 'UPDATE', 'ALL')\n  AND with_check IS NULL;</code></pre>\n\n<p><strong>Fix:</strong> Add <code>WITH CHECK</code> to every INSERT and UPDATE policy.</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY \"users insert own data\" ON public.profiles\n  FOR INSERT WITH CHECK (auth.uid() = user_id);\n\nCREATE POLICY \"users update own data\" ON public.profiles\n  FOR UPDATE USING (auth.uid() = user_id)\n  WITH CHECK (auth.uid() = user_id);</code></pre>\n\n<h3>4. Service Role Key in Frontend Code</h3>\n\n<p>Supabase provides two API keys. The <code>anon</code> key respects RLS policies. The <code>service_role</code> key bypasses all RLS entirely. If the <code>service_role</code> key appears in your frontend code, every user of your application has unrestricted access to your entire database. No policies matter. No row-level restrictions apply.</p>\n\n<p><strong>Fix:</strong> Search your entire frontend codebase for your service role key. Check environment variables, config files, and bundled output. The <code>service_role</code> key must only ever be used in server-side code: API routes, Edge Functions, or server components that never send the key to the browser.</p>\n\n<h3>5. Public Storage Buckets for User Data</h3>\n\n<p>Public storage buckets serve files to anyone with the URL. No authentication, no authorization, no access check on reads. If your application stores user uploads, profile photos, documents, or any user-generated content in a public bucket, those files are accessible to the entire internet.</p>\n\n<p>Public bucket URLs follow a predictable pattern, making enumeration straightforward. We covered this attack surface in depth in <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>.</p>\n\n<pre><code class=\"language-sql\">-- Check bucket visibility\nSELECT id, name, public FROM storage.buckets;</code></pre>\n\n<p><strong>Fix:</strong> Create a new private bucket, migrate files, and use signed URLs for access. See the <a href=\"/blog/supabase-storage-buckets-probably-public\">storage security guide</a> for the complete migration process.</p>\n\n<h3>6. No Policies on storage.objects</h3>\n\n<p>Even with private buckets, the <code>storage.objects</code> table needs its own RLS policies. Without them, upload and delete operations either fail entirely (if RLS is enforced) or are uncontrolled (if someone added an overly permissive policy to get uploads working).</p>\n\n<p><strong>Fix:</strong> Add folder-based storage policies that scope access to the file owner.</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY \"Users upload to own folder\" ON storage.objects\n  FOR INSERT WITH CHECK (\n    bucket_id = 'user-uploads'\n    AND auth.uid()::text = (storage.foldername(name))[1]\n  );\n\nCREATE POLICY \"Users read own files\" ON storage.objects\n  FOR SELECT USING (\n    bucket_id = 'user-uploads'\n    AND auth.uid()::text = (storage.foldername(name))[1]\n  );</code></pre>\n\n<h3>7. Email Confirmation Disabled</h3>\n\n<p>Without email confirmation, anyone can sign up with any email address -- including addresses they do not own. This enables account impersonation, fake signups at scale, and abuse of any feature gated by email identity.</p>\n\n<p><strong>Fix:</strong> Enable email confirmation in the Supabase dashboard under Authentication > Settings. For production applications, this is non-negotiable.</p>\n\n<h3>8. Incomplete Policy Coverage Across Operations</h3>\n\n<p>A table might have a SELECT policy but no INSERT, UPDATE, or DELETE policies. This means reads are controlled but writes are either blocked (confusing developers) or uncontrolled (if someone adds a broad policy later). Every operation your application performs on a table needs a corresponding policy.</p>\n\n<pre><code class=\"language-sql\">-- Check operation coverage per table\nSELECT tablename, ARRAY_AGG(DISTINCT cmd) AS covered_operations\nFROM pg_policies WHERE schemaname = 'public'\nGROUP BY tablename ORDER BY tablename;</code></pre>\n\n<p><strong>Fix:</strong> Map every database operation your application performs. Add policies for each one. Use the <a href=\"/blog/how-to-audit-supabase-rls-policies\">step-by-step RLS audit guide</a> to systematically check coverage.</p>\n\n<h3>9. Missing Indexes on Policy Columns</h3>\n\n<p>RLS policies evaluate their conditions on every query. If the columns referenced in <code>USING</code> and <code>WITH CHECK</code> clauses lack indexes, every query triggers a full table scan. On tables with significant data, response times degrade from milliseconds to seconds. The practical result: developers disable RLS for performance, creating a security vulnerability.</p>\n\n<pre><code class=\"language-sql\">CREATE INDEX idx_your_table_user_id ON public.your_table (user_id);</code></pre>\n\n<h3>10. Exposing the Data API When Unused</h3>\n\n<p>Supabase auto-generates REST and GraphQL endpoints for every table in the <code>public</code> schema. If your application only accesses the database through server-side code or Edge Functions, these APIs are an unnecessary attack surface. Every exposed endpoint is a potential entry point.</p>\n\n<p><strong>Fix:</strong> If your application works without the auto-generated APIs, disable them in the Supabase dashboard under API Settings.</p>\n\n<h2>How to Audit Your RLS Policies</h2>\n\n<p>A one-time security check is not enough. Every new table, migration, and feature changes your security posture. You need a repeatable audit process.</p>\n\n<p>We published a complete <a href=\"/blog/how-to-audit-supabase-rls-policies\">step-by-step RLS audit guide</a> that walks through nine checks using SQL queries you can run directly in the Supabase SQL Editor. The process covers:</p>\n\n<ol>\n<li>Inventorying all public tables</li>\n<li>Verifying RLS is enabled on every table</li>\n<li>Mapping all existing policies</li>\n<li>Identifying tables with RLS enabled but no policies</li>\n<li>Checking operation coverage (SELECT, INSERT, UPDATE, DELETE)</li>\n<li>Finding overly permissive conditions (<code>USING (true)</code>)</li>\n<li>Verifying <code>WITH CHECK</code> clauses exist on write operations</li>\n<li>Checking for missing indexes on policy columns</li>\n<li>Testing policies with role switching</li>\n</ol>\n\n<p>For a small project, a manual audit takes 30 to 60 minutes. For larger applications with dozens of tables, it can take half a day. The key is making this audit part of your development workflow, not a one-time pre-launch activity.</p>\n\n<h2>Authentication and Authorization Best Practices</h2>\n\n<p>RLS handles data access control, but authentication determines <em>who</em> is making the request. A misconfigured auth setup undermines even the strongest RLS policies.</p>\n\n<h3>Enforce Email Verification</h3>\n\n<p>Enable email confirmation for all email/password signups. Without it, anyone can create an account with an unverified email address. This is the minimum for any production application.</p>\n\n<h3>Configure MFA for Sensitive Operations</h3>\n\n<p>Supabase supports TOTP-based multi-factor authentication. For applications handling financial data, personal information, or any regulated content, MFA should be required -- not optional. Implement MFA enrollment during onboarding and enforce it for sensitive operations like account deletion or data export.</p>\n\n<h3>Validate OAuth Redirect URLs</h3>\n\n<p>If your application uses third-party OAuth providers (Google, GitHub, Discord), ensure redirect URLs are configured to exact matches. Wildcard or overly broad redirect patterns can enable token theft through open redirect attacks. Only enable providers your application actively uses -- each enabled provider is an additional attack surface.</p>\n\n<h3>Restrict Signups by Domain</h3>\n\n<p>For internal tools or B2B applications, restrict signups to allowed email domains. Implement this with a database function and trigger on the <code>auth.users</code> table, or use Supabase's built-in domain restrictions. This prevents unauthorized access before it reaches your RLS layer.</p>\n\n<h3>Use Short-Lived Sessions</h3>\n\n<p>Configure JWT expiration times appropriate for your application's risk profile. Shorter sessions reduce the window of exposure if a token is compromised. For high-security applications, combine short JWT expiration with refresh token rotation.</p>\n\n<h2>API Key Management and Exposure Risks</h2>\n\n<p>Supabase projects use two API keys with fundamentally different privilege levels. Understanding the distinction is critical.</p>\n\n<h3>The anon Key: Safe for Frontend Use</h3>\n\n<p>The <code>anon</code> key is designed to be embedded in frontend code. All requests using this key are filtered through RLS policies. It cannot bypass row-level restrictions. This key is not a secret -- it is a public identifier that works only because RLS policies control what it can access.</p>\n\n<p>This is why RLS configuration is so important. The anon key is only safe because RLS is enforced. If RLS is disabled or misconfigured, the anon key becomes a master key to your entire database.</p>\n\n<h3>The service_role Key: Server-Only, Always</h3>\n\n<p>The <code>service_role</code> key bypasses all RLS policies. It has full, unrestricted access to every table, every row, every operation. This key must never appear in frontend code, client-side environment variables, or any context where it could be extracted by a user.</p>\n\n<p>Use it exclusively in:</p>\n<ul>\n<li>Server-side API routes (Next.js <code>app/api/</code> or <code>pages/api/</code>)</li>\n<li>Supabase Edge Functions</li>\n<li>Server components that never send the key to the client</li>\n<li>Backend services and cron jobs</li>\n</ul>\n\n<h3>Environment Variable Hygiene</h3>\n\n<p>In Next.js, only environment variables prefixed with <code>NEXT_PUBLIC_</code> are bundled into client-side code. Your service role key should never have this prefix. Use <code>SUPABASE_SERVICE_ROLE_KEY</code> (server-only) and <code>NEXT_PUBLIC_SUPABASE_ANON_KEY</code> (client-safe).</p>\n\n<p>Audit your environment configuration across all deployment environments: local development, staging, and production. A service role key that is correctly configured locally but exposed in a CI/CD pipeline or hosting provider dashboard is still a vulnerability.</p>\n\n<h3>Key Rotation</h3>\n\n<p>If you suspect a key has been compromised -- or if a team member with access leaves -- rotate your API keys immediately through the Supabase dashboard. Update all environment variables and redeploy. The old keys become invalid immediately, so coordinate the rotation with a deployment.</p>\n\n<h2>Putting It All Together: A Security Action Plan</h2>\n\n<p>If you have read this far, you understand the threat landscape. Here is how to act on it, in priority order:</p>\n\n<p><strong>Immediate (do today):</strong></p>\n<ol>\n<li>Run the <a href=\"/blog/how-to-audit-supabase-rls-policies\">RLS audit</a> on your production database</li>\n<li>Enable RLS on any unprotected tables</li>\n<li>Search your frontend codebase for the service role key</li>\n<li>Check storage bucket visibility</li>\n</ol>\n\n<p><strong>This week:</strong></p>\n<ol>\n<li>Replace any <code>USING (true)</code> policies with proper ownership checks</li>\n<li>Add <code>WITH CHECK</code> to all INSERT and UPDATE policies</li>\n<li>Enable email confirmation if it is off</li>\n<li>Add indexes to all columns referenced in RLS policies</li>\n</ol>\n\n<p><strong>Ongoing:</strong></p>\n<ol>\n<li>Re-audit after every schema change or migration</li>\n<li>Review auth configuration quarterly</li>\n<li>Rotate API keys when team membership changes</li>\n<li>Use the <a href=\"/blog/supabase-security-checklist-production\">production security checklist</a> before every launch</li>\n</ol>\n\n<h2>Automate Your Security Posture</h2>\n\n<p>Manual auditing works, but it does not scale with the speed of modern development. Every new table, every schema migration, every AI-generated feature can introduce new vulnerabilities. If your team ships weekly -- or daily -- manual audits fall behind.</p>\n\n<p>SupaScanner automates the entire audit process described in this guide. It connects to your Supabase project using read-only credentials and checks every table for RLS status, analyzes all policies for completeness and permissiveness, audits storage buckets and auth configuration, verifies index coverage, and generates copy-paste SQL fixes for every issue found.</p>\n\n<p>The scan completes in under 40 seconds. You get an A-through-F security grade, a prioritized list of findings, and the exact SQL to fix each one. No manual queries, no cross-referencing system catalogs, no wondering if you missed something.</p>\n\n<p>For a deeper look at why automated scanning is particularly important for AI-assisted development workflows, read <a href=\"/blog/vibe-coding-automated-security-scanning\">Why Vibe Coding Needs Automated Security Scanning</a>.</p>\n\n<p>The Supabase platform provides strong security primitives. Whether your application is actually secure depends entirely on how those primitives are configured. This guide gives you the knowledge. The tools linked throughout give you the process. What remains is doing the work.</p>\n\n<p>Your users' data depends on it.</p>\n\n<p><strong><a href=\"/login\">Scan your Supabase project for free and get your security grade --></a></strong></p>",
  },
  {
    slug: "top-10-supabase-security-mistakes",
    title: "Top 10 Supabase Security Mistakes (And How to Fix Each One)",
    description:
      "The 10 most dangerous Supabase security mistakes developers make -- from disabled RLS and exposed keys to missing MFA and no audit logging. Includes bad vs. good code examples and a self-assessment to check your project.",
    category: "Security Basics",
    publishedAt: "2026-03-30",
    keywords: [
      "supabase security",
      "supabase RLS",
      "supabase security best practices",
      "supabase security mistakes",
      "supabase security checklist",
      "supabase edge function security",
    ],
    body: "<p>Every Supabase project ships with the same security primitives. And most Supabase projects misconfigure them in the same ways.</p>\n\n<p>We have scanned hundreds of Supabase applications and the same 10 mistakes appear over and over. Some are well-known. Others -- like missing rate limits on Edge Functions or unvalidated webhooks -- fly under the radar until an attacker finds them first.</p>\n\n<p>This is not a theory piece. For each mistake, you get: what goes wrong, why it matters, the vulnerable code, the fixed code, and a link to scan your project for that exact issue. As we documented in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>, these are the patterns that cause real data exposures.</p>\n\n<h2>The 10 Mistakes</h2>\n\n<h3>1. Disabled RLS on Public Tables</h3>\n\n<p><strong>The risk:</strong> Every table in the <code>public</code> schema is exposed through Supabase's auto-generated REST API. Without Row Level Security, anyone with your project URL and <code>anon</code> key -- both embedded in your frontend JavaScript -- can read, write, and delete every row.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-sql\">-- Table created without RLS (or explicitly disabled during dev)\nCREATE TABLE public.user_profiles (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  user_id UUID REFERENCES auth.users(id),\n  full_name TEXT,\n  email TEXT,\n  phone TEXT\n);\nALTER TABLE public.user_profiles DISABLE ROW LEVEL SECURITY;</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-sql\">CREATE TABLE public.user_profiles (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  user_id UUID REFERENCES auth.users(id),\n  full_name TEXT,\n  email TEXT,\n  phone TEXT\n);\nALTER TABLE public.user_profiles ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.user_profiles FORCE ROW LEVEL SECURITY;\n\nCREATE POLICY &quot;Users read own profile&quot;\n  ON public.user_profiles FOR SELECT\n  USING (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users update own profile&quot;\n  ON public.user_profiles FOR UPDATE\n  USING (auth.uid() = user_id)\n  WITH CHECK (auth.uid() = user_id);</code></pre>\n\n<p>For a deep dive into RLS policy patterns, read <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<h3>2. Exposed service_role Key in Client Code</h3>\n\n<p><strong>The risk:</strong> The <code>service_role</code> key bypasses all RLS policies. If it appears anywhere in your frontend bundle, every user of your application has unrestricted admin access to your entire database. This is the single most dangerous misconfiguration possible.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-javascript\">// .env.local -- WRONG: NEXT_PUBLIC_ prefix exposes this to the browser\nNEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUz...\n\n// lib/supabase.ts -- shipped to every user's browser\nimport { createClient } from '@supabase/supabase-js';\nexport const supabase = createClient(\n  process.env.NEXT_PUBLIC_SUPABASE_URL!,\n  process.env.NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY! // full DB access\n);</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-javascript\">// .env.local -- anon key for client, service_role for server only\nNEXT_PUBLIC_SUPABASE_URL=https://abc.supabase.co\nNEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...  // safe for frontend\nSUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...       // server-only, no prefix\n\n// lib/supabase-client.ts -- browser-safe\nexport const supabase = createClient(\n  process.env.NEXT_PUBLIC_SUPABASE_URL!,\n  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!   // respects RLS\n);\n\n// lib/supabase-admin.ts -- server-only (API routes, Edge Functions)\nexport const supabaseAdmin = createClient(\n  process.env.NEXT_PUBLIC_SUPABASE_URL!,\n  process.env.SUPABASE_SERVICE_ROLE_KEY!        // never reaches browser\n);</code></pre>\n\n<p>Search your entire frontend build output for your service role key. If you find it, you have a critical vulnerability that no amount of RLS will protect against.</p>\n\n<h3>3. Missing Auth Policies on Sensitive Tables</h3>\n\n<p><strong>The risk:</strong> RLS is enabled, but no policies exist. The table blocks all access, which seems safe -- until a developer adds a <code>USING (true)</code> policy to unblock themselves. Or worse, disables RLS entirely. Tables without policies are ticking time bombs.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-sql\">-- RLS enabled but zero policies = all queries return empty\n-- Developer reaction: \"RLS is broken, let me disable it\"\nALTER TABLE public.orders ENABLE ROW LEVEL SECURITY;\n-- No policies created. App appears broken. Developer panics.</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-sql\">ALTER TABLE public.orders ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.orders FORCE ROW LEVEL SECURITY;\n\n-- Explicit policies for every operation the app performs\nCREATE POLICY &quot;Users read own orders&quot;\n  ON public.orders FOR SELECT\n  USING (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users create own orders&quot;\n  ON public.orders FOR INSERT\n  WITH CHECK (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users update own orders&quot;\n  ON public.orders FOR UPDATE\n  USING (auth.uid() = user_id)\n  WITH CHECK (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users delete own orders&quot;\n  ON public.orders FOR DELETE\n  USING (auth.uid() = user_id);</code></pre>\n\n<p>Use our <a href=\"/blog/how-to-audit-supabase-rls-policies\">step-by-step RLS audit guide</a> to find every table missing policies.</p>\n\n<h3>4. Public Storage Buckets with User Data</h3>\n\n<p><strong>The risk:</strong> Public buckets serve files to anyone who knows the URL. No authentication, no authorization check on reads. If your users upload profile photos, documents, or any personal files to a public bucket, those files are on the open internet. URLs are predictable and enumerable.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-javascript\">// Creating a public bucket -- every file is world-readable\nconst { data, error } = await supabaseAdmin\n  .storage\n  .createBucket('user-documents', { public: true });\n\n// Anyone can access: /storage/v1/object/public/user-documents/invoice_001.pdf</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-javascript\">// Private bucket -- reads require authentication\nconst { data, error } = await supabaseAdmin\n  .storage\n  .createBucket('user-documents', { public: false });\n\n// Generate time-limited signed URL for authorized access\nconst { data: signedUrl } = await supabase.storage\n  .from('user-documents')\n  .createSignedUrl('invoices/invoice_001.pdf', 300); // 5 min expiry</code></pre>\n\n<p>For the full migration playbook, read <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>.</p>\n\n<h3>5. No Rate Limiting on Edge Functions</h3>\n\n<p><strong>The risk:</strong> Supabase Edge Functions are publicly accessible HTTP endpoints. Without rate limiting, attackers can abuse them for credential stuffing, resource exhaustion, or cost inflation. A single script can call your function thousands of times per second.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-typescript\">// supabase/functions/send-invite/index.ts\n// No rate limiting -- anyone can call this unlimited times\nimport { serve } from 'https://deno.land/std/http/server.ts';\n\nserve(async (req) =&gt; {\n  const { email } = await req.json();\n  // Send invite email directly -- no abuse protection\n  await sendEmail(email, 'You are invited!');\n  return new Response(JSON.stringify({ success: true }));\n});</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-typescript\">// supabase/functions/send-invite/index.ts\nimport { serve } from 'https://deno.land/std/http/server.ts';\nimport { createClient } from 'https://esm.sh/@supabase/supabase-js';\n\nconst RATE_LIMIT_WINDOW_MS = 60_000; // 1 minute\nconst MAX_REQUESTS = 5;\n\nserve(async (req) =&gt; {\n  const supabase = createClient(\n    Deno.env.get('SUPABASE_URL')!,\n    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!\n  );\n\n  // Get caller identity from JWT\n  const authHeader = req.headers.get('Authorization');\n  const { data: { user } } = await supabase.auth.getUser(\n    authHeader?.replace('Bearer ', '')\n  );\n  if (!user) return new Response('Unauthorized', { status: 401 });\n\n  // Check rate limit using a database counter\n  const windowStart = new Date(Date.now() - RATE_LIMIT_WINDOW_MS).toISOString();\n  const { count } = await supabase\n    .from('function_rate_limits')\n    .select('*', { count: 'exact', head: true })\n    .eq('user_id', user.id)\n    .eq('function_name', 'send-invite')\n    .gte('called_at', windowStart);\n\n  if ((count ?? 0) &gt;= MAX_REQUESTS) {\n    return new Response('Rate limit exceeded', { status: 429 });\n  }\n\n  // Log the call and proceed\n  await supabase.from('function_rate_limits').insert({\n    user_id: user.id, function_name: 'send-invite'\n  });\n\n  const { email } = await req.json();\n  await sendEmail(email, 'You are invited!');\n  return new Response(JSON.stringify({ success: true }));\n});</code></pre>\n\n<h3>6. Default Auth Settings Unchanged</h3>\n\n<p><strong>The risk:</strong> Supabase ships with permissive defaults to make development easy. Email confirmation is disabled by default. JWT expiry is long. Password requirements are minimal. These defaults are fine for prototyping -- and dangerous for production.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-text\">Supabase Dashboard &gt; Authentication &gt; Settings:\n- Enable email confirmations: OFF\n- Minimum password length: 6\n- JWT expiry: 3600 (1 hour)\n- Enable new user signups: ON (unrestricted)</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-text\">Supabase Dashboard &gt; Authentication &gt; Settings:\n- Enable email confirmations: ON\n- Minimum password length: 10\n- JWT expiry: 900 (15 minutes) with refresh token rotation\n- Enable new user signups: ON with email domain restrictions\n- Enable manual linking: OFF (prevents account takeover via OAuth)</code></pre>\n\n<p>Every one of these settings is a checkbox in the Supabase dashboard. There is no excuse for leaving them at defaults in production.</p>\n\n<h3>7. Unvalidated Webhook Endpoints</h3>\n\n<p><strong>The risk:</strong> Supabase Database Webhooks and Auth Hooks send HTTP requests to your endpoints when events occur. If your endpoint does not validate the request origin, anyone can forge webhook payloads to trigger actions -- creating fake users, confirming fake payments, or injecting data.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-typescript\">// app/api/webhooks/new-user/route.ts\n// Trusts any incoming request as a legitimate Supabase webhook\nexport async function POST(req: Request) {\n  const payload = await req.json();\n  // No signature verification -- anyone can POST to this endpoint\n  await provisionAccount(payload.record.id);\n  return Response.json({ success: true });\n}</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-typescript\">// app/api/webhooks/new-user/route.ts\nimport { createHmac, timingSafeEqual } from 'crypto';\n\nconst WEBHOOK_SECRET = process.env.SUPABASE_WEBHOOK_SECRET!;\n\nexport async function POST(req: Request) {\n  // Verify the webhook signature\n  const signature = req.headers.get('x-webhook-signature');\n  if (!signature) return new Response('Missing signature', { status: 401 });\n\n  const body = await req.text();\n  const expected = createHmac('sha256', WEBHOOK_SECRET)\n    .update(body)\n    .digest('hex');\n\n  const sigBuffer = Buffer.from(signature, 'hex');\n  const expectedBuffer = Buffer.from(expected, 'hex');\n\n  if (\n    sigBuffer.length !== expectedBuffer.length ||\n    !timingSafeEqual(sigBuffer, expectedBuffer)\n  ) {\n    return new Response('Invalid signature', { status: 403 });\n  }\n\n  const payload = JSON.parse(body);\n  await provisionAccount(payload.record.id);\n  return Response.json({ success: true });\n}</code></pre>\n\n<p>Always use a shared secret and HMAC verification. Use <code>timingSafeEqual</code> to prevent timing attacks on the comparison.</p>\n\n<h3>8. Missing MFA Enforcement for Admin Roles</h3>\n\n<p><strong>The risk:</strong> Admin accounts have elevated access -- managing users, viewing all data, configuring the application. Without multi-factor authentication, a compromised password gives an attacker full admin access. Supabase supports TOTP-based MFA, but it must be explicitly enforced in your application logic.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-typescript\">// app/api/admin/users/route.ts\n// Admin endpoint with no MFA check\nexport async function GET(req: Request) {\n  const supabase = createServerClient();\n  const { data: { user } } = await supabase.auth.getUser();\n  if (!user) return new Response('Unauthorized', { status: 401 });\n\n  // Only checks role, not MFA status\n  const { data: profile } = await supabase\n    .from('profiles')\n    .select('role')\n    .eq('user_id', user.id)\n    .single();\n\n  if (profile?.role !== 'admin') {\n    return new Response('Forbidden', { status: 403 });\n  }\n\n  // Returns all users -- no MFA gate on this sensitive operation\n  const { data: users } = await supabase.from('profiles').select('*');\n  return Response.json(users);\n}</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-typescript\">// app/api/admin/users/route.ts\nexport async function GET(req: Request) {\n  const supabase = createServerClient();\n  const { data: { user } } = await supabase.auth.getUser();\n  if (!user) return new Response('Unauthorized', { status: 401 });\n\n  // Check role\n  const { data: profile } = await supabase\n    .from('profiles')\n    .select('role')\n    .eq('user_id', user.id)\n    .single();\n\n  if (profile?.role !== 'admin') {\n    return new Response('Forbidden', { status: 403 });\n  }\n\n  // Verify MFA is enrolled and current session used it\n  const { data: factors } = await supabase.auth.mfa.listFactors();\n  const verifiedFactors = factors?.totp?.filter(\n    (f) =&gt; f.status === 'verified'\n  );\n\n  if (!verifiedFactors || verifiedFactors.length === 0) {\n    return Response.json(\n      { error: 'MFA enrollment required for admin access' },\n      { status: 403 }\n    );\n  }\n\n  // Check that the current session's AAL meets AAL2 (MFA verified)\n  const { data: aal } = await supabase.auth.mfa.getAuthenticatorAssuranceLevel();\n  if (aal?.currentLevel !== 'aal2') {\n    return Response.json(\n      { error: 'MFA verification required', nextStep: '/auth/mfa-verify' },\n      { status: 403 }\n    );\n  }\n\n  const { data: users } = await supabase.from('profiles').select('*');\n  return Response.json(users);\n}</code></pre>\n\n<h3>9. Overly Permissive CORS Configuration</h3>\n\n<p><strong>The risk:</strong> If your Edge Functions or API routes accept requests from any origin, attackers can make authenticated requests from malicious sites using your users' credentials. This enables cross-site request forgery and data exfiltration from authenticated sessions.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-typescript\">// supabase/functions/my-api/index.ts\nserve(async (req) =&gt; {\n  // Allows ANY website to call this function with user credentials\n  const headers = {\n    'Access-Control-Allow-Origin': '*',\n    'Access-Control-Allow-Headers': '*',\n    'Access-Control-Allow-Methods': '*',\n  };\n\n  if (req.method === 'OPTIONS') {\n    return new Response(null, { headers });\n  }\n\n  // ... function logic\n  return new Response(JSON.stringify(data), { headers });\n});</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-typescript\">// supabase/functions/my-api/index.ts\nconst ALLOWED_ORIGINS = [\n  'https://supabase-scanner.vercel.app',\n  'https://supascanner.com',\n];\n\nserve(async (req) =&gt; {\n  const origin = req.headers.get('Origin') ?? '';\n  const isAllowed = ALLOWED_ORIGINS.includes(origin);\n\n  const corsHeaders = {\n    'Access-Control-Allow-Origin': isAllowed ? origin : '',\n    'Access-Control-Allow-Headers': 'Authorization, Content-Type',\n    'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',\n    'Access-Control-Max-Age': '86400',\n  };\n\n  if (req.method === 'OPTIONS') {\n    return new Response(null, { status: 204, headers: corsHeaders });\n  }\n\n  // ... function logic\n  return new Response(JSON.stringify(data), { headers: corsHeaders });\n});</code></pre>\n\n<p>Never use <code>*</code> for <code>Access-Control-Allow-Origin</code> on endpoints that accept credentials. Whitelist your production domains explicitly.</p>\n\n<h3>10. No Audit Logging Enabled</h3>\n\n<p><strong>The risk:</strong> Without audit logging, you have no visibility into who accessed what, when changes were made, or what happened during a security incident. When something goes wrong -- and eventually it will -- you are flying blind. You cannot investigate breaches, prove compliance, or even detect unauthorized access.</p>\n\n<p><strong>Bad:</strong></p>\n\n<pre><code class=\"language-sql\">-- No logging. No visibility. No trail.\n-- If data is accessed or modified, you will never know by whom or when.</code></pre>\n\n<p><strong>Good:</strong></p>\n\n<pre><code class=\"language-sql\">-- Create an audit log table\nCREATE TABLE public.audit_log (\n  id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n  table_name TEXT NOT NULL,\n  operation TEXT NOT NULL,\n  user_id UUID,\n  old_data JSONB,\n  new_data JSONB,\n  created_at TIMESTAMPTZ DEFAULT now()\n);\n\nALTER TABLE public.audit_log ENABLE ROW LEVEL SECURITY;\n-- Only service_role can write; admins can read\nCREATE POLICY &quot;Service role inserts audit logs&quot;\n  ON public.audit_log FOR INSERT\n  WITH CHECK (auth.role() = 'service_role');\n\n-- Generic audit trigger function\nCREATE OR REPLACE FUNCTION public.audit_trigger()\nRETURNS TRIGGER AS $$\nBEGIN\n  INSERT INTO public.audit_log (table_name, operation, user_id, old_data, new_data)\n  VALUES (\n    TG_TABLE_NAME,\n    TG_OP,\n    auth.uid(),\n    CASE WHEN TG_OP IN ('UPDATE', 'DELETE') THEN to_jsonb(OLD) END,\n    CASE WHEN TG_OP IN ('INSERT', 'UPDATE') THEN to_jsonb(NEW) END\n  );\n  RETURN COALESCE(NEW, OLD);\nEND;\n$$ LANGUAGE plpgsql SECURITY DEFINER;\n\n-- Attach to sensitive tables\nCREATE TRIGGER audit_user_profiles\n  AFTER INSERT OR UPDATE OR DELETE ON public.user_profiles\n  FOR EACH ROW EXECUTE FUNCTION public.audit_trigger();\n\nCREATE TRIGGER audit_orders\n  AFTER INSERT OR UPDATE OR DELETE ON public.orders\n  FOR EACH ROW EXECUTE FUNCTION public.audit_trigger();</code></pre>\n\n<p>At minimum, log all writes to tables containing user data, financial records, or access control information.</p>\n\n<h2>How Many Apply to Your Project?</h2>\n\n<p>Be honest. As you read through these 10 mistakes, how many made you think \"that might be us\"?</p>\n\n<p>Here is a quick self-assessment:</p>\n\n<ol>\n<li>Have you verified RLS is enabled on <strong>every</strong> public table?</li>\n<li>Have you searched your frontend build for the <code>service_role</code> key?</li>\n<li>Does every RLS-enabled table have policies for all operations your app performs?</li>\n<li>Are your storage buckets set to private for user-uploaded content?</li>\n<li>Do your Edge Functions enforce rate limits?</li>\n<li>Have you changed auth defaults -- email confirmation, password length, JWT expiry?</li>\n<li>Do your webhook endpoints verify request signatures?</li>\n<li>Is MFA required for admin-level operations?</li>\n<li>Are your CORS headers scoped to specific allowed origins?</li>\n<li>Do you have audit logging on sensitive tables?</li>\n</ol>\n\n<p>If you answered \"no\" or \"not sure\" to even one of these, your project has a security gap. If you answered that way to three or more, you are in the majority -- but that does not make it safe.</p>\n\n<p><strong>Find out exactly which ones affect your project.</strong> SupaScanner checks all 10 of these patterns automatically and generates the exact SQL and code fixes. The scan takes under 60 seconds.</p>\n\n<p><strong><a href=\"/login?utm_source=blog&amp;utm_medium=content&amp;utm_campaign=top-10-mistakes\">Scan your Supabase project now -- free, 60 seconds, no credit card --></a></strong></p>\n\n<h2>What to Do Next</h2>\n\n<p>If you want to fix these manually, work through them in order. The first four are the most critical -- they cover the attack surface that is actively exploited in the wild. Our <a href=\"/blog/supabase-security-checklist-production\">production security checklist</a> provides a systematic 15-point walkthrough, and the <a href=\"/blog/how-to-audit-supabase-rls-policies\">RLS audit guide</a> covers the database layer in detail.</p>\n\n<p>If you are building with AI-assisted tools, read <a href=\"/blog/vibe-coding-automated-security-scanning\">Why Vibe Coding Needs Automated Security Scanning</a>. The code AI generates is optimized for functionality, not security. Every one of the 10 mistakes above is a common output of AI-generated Supabase code.</p>\n\n<p>Or skip the manual work entirely. Connect your project, run the scan, and get a prioritized list of findings with copy-paste fixes in under a minute.</p>\n\n<p><strong><a href=\"/login?utm_source=blog&amp;utm_medium=content&amp;utm_campaign=top-10-mistakes\">Get your free security scan --></a></strong></p>",
  },
  {
    slug: "best-supabase-security-tools-compared",
    title: "Best Supabase Security Tools Compared (2026)",
    description:
      "Compare 7+ Supabase security tools side by side. Feature matrix covering RLS scanning, storage checks, key detection, and CI/CD integration for every team size.",
    category: "Guides",
    publishedAt: "2026-04-15",
    keywords: ["supabase security tools","supabase security scanner comparison","supabase security","supabase scanner"],
    faqs: [
      { question: "Do I need a security scanner if I wrote my RLS policies myself?", answer: "Yes. Manual RLS policies are better than none, but they are easy to get wrong. A scanner catches missing policies on new tables, overly permissive SELECT rules, and gaps you did not think to test." },
      { question: "Can I use multiple tools together?", answer: "Absolutely. Many teams run a full-stack scanner like SupaSecScan in CI/CD and use SupaExplorer for quick deployed-app checks. The tools are complementary, not competitive." },
      { question: "Are these tools safe to run against my production database?", answer: "The scanners listed here (except SupaRalph) run non-destructive, read-only checks. SupaRalph performs active exploit tests -- run it against a staging environment first." },
      { question: "How often should I scan?", answer: "Every PR at minimum. Your security posture changes with every schema migration, every new table, and every RLS policy update. Continuous scanning catches drift." },
    ],
    body: "<p>Supabase powers thousands of production apps, and most of them have at least one security gap. Missing RLS policies, public storage buckets, leaked service role keys -- the attack surface is real and growing. The good news: a wave of security tools now exists to catch these problems before attackers do.</p>\n<p>The bad news: choosing between them is not straightforward. Some scan RLS policies. Others focus on API key exposure. A few cover the full stack. This guide compares every major Supabase security tool available in 2026 so you can pick the right one for your project.</p>\n<h2>What to Look for in a Supabase Security Scanner</h2>\n<p>Before diving into individual tools, here is what matters when evaluating a Supabase security scanner:</p>\n<ul>\n<li><strong>RLS policy coverage</strong> -- Can it detect missing or misconfigured Row Level Security policies? This is the single most common Supabase vulnerability.</li>\n<li><strong>Storage bucket checks</strong> -- Does it flag <a href=\"/blog/supabase-storage-buckets-probably-public\">public buckets that should be private</a>?</li>\n<li><strong>Auth configuration audit</strong> -- Can it review your authentication settings, email confirmations, and OAuth providers?</li>\n<li><strong>API key detection</strong> -- Does it scan for leaked service role keys in your frontend bundle?</li>\n<li><strong>CI/CD integration</strong> -- Can you run it automatically on every pull request or deployment?</li>\n<li><strong>Ease of use</strong> -- Does it require deep Supabase knowledge, or can a developer new to the platform run it?</li>\n<li><strong>Pricing</strong> -- Free tier? Per-project pricing? Enterprise options?</li>\n</ul>\n<h2>The Tools</h2>\n<h3>SupaSecScan</h3>\n<p>SupaSecScan is a full-stack Supabase security scanner that checks RLS policies, storage bucket permissions, auth configuration, and API key exposure in a single scan. It runs as a web app, a CLI tool, and a GitHub Action.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>RLS policy scanning across all tables</li>\n<li>Storage bucket permission analysis</li>\n<li>Auth configuration review</li>\n<li>API key leak detection in frontend bundles</li>\n<li>MFA and email confirmation checks</li>\n</ul>\n<p><strong>How it works:</strong> Point it at your Supabase project URL and anon key. The scanner runs a non-destructive audit and returns a prioritized list of findings with severity ratings and fix recommendations.</p>\n<p><strong>Best for:</strong> Teams that want one tool covering the full Supabase security surface. The GitHub Action integration makes it practical for CI/CD pipelines where you want every PR checked automatically.</p>\n<p><strong>Pricing:</strong> Free scan available. Paid tiers for continuous monitoring and team features.</p>\n<h3>VibeAppScanner</h3>\n<p>VibeAppScanner focuses on RLS, storage, and RPC testing. It positions itself as a fast scanner that completes in under five minutes and targets developers who built their apps with AI code generators.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>RLS policy validation</li>\n<li>Storage permission testing</li>\n<li>RPC endpoint security checks</li>\n</ul>\n<p><strong>How it works:</strong> Connects to your Supabase project and runs targeted tests against your RLS policies, storage buckets, and RPC functions. Results are delivered as a pass/fail report.</p>\n<p><strong>Best for:</strong> Developers who primarily need RLS and storage validation and want results fast. Strong content around <a href=\"/blog/vibe-coding-automated-security-scanning\">vibe coding security</a>, which suggests good awareness of the AI-generated code problem.</p>\n<p><strong>Pricing:</strong> Check their website for current pricing.</p>\n<h3>AuditYourApp</h3>\n<p>AuditYourApp is a dual-platform scanner that covers both Supabase and Firebase projects. If your stack spans multiple backend-as-a-service platforms, this is the only tool that handles both.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>Supabase security scanning</li>\n<li>Firebase security scanning</li>\n<li>Cross-platform security assessment</li>\n</ul>\n<p><strong>How it works:</strong> Supports both Supabase and Firebase projects from a single interface. Runs security checks tailored to each platform.</p>\n<p><strong>Best for:</strong> Teams running both Supabase and Firebase, or agencies managing multiple client projects across platforms. The dual-platform support is unique in this space.</p>\n<p><strong>Pricing:</strong> Check their website for current pricing.</p>\n<h3>SupaExplorer</h3>\n<p>SupaExplorer takes a different approach: instead of scanning your project configuration, it detects leaked Supabase API keys in deployed frontends. It ships as a Chrome extension.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>Leaked API key detection in browser</li>\n<li>Supabase project discovery via exposed keys</li>\n<li>Frontend bundle analysis</li>\n</ul>\n<p><strong>How it works:</strong> The Chrome extension scans pages you visit for exposed Supabase keys. It identifies anon keys (expected) versus service role keys (catastrophic) and flags dangerous exposures.</p>\n<p><strong>Best for:</strong> Security researchers and developers who want to quickly check if a deployed application is leaking sensitive keys. Not a replacement for a full security audit, but useful for a specific and dangerous class of vulnerability.</p>\n<p><strong>Pricing:</strong> Free Chrome extension.</p>\n<h3>Supaguard</h3>\n<p>Supaguard positions itself as a Supabase security monitoring platform. It currently ranks on page one for several Supabase security keywords, making it one of the more visible tools in this space.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>Supabase security monitoring</li>\n<li>Ongoing security assessment</li>\n</ul>\n<p><strong>How it works:</strong> Provides security monitoring for Supabase projects with a focus on continuous assessment rather than one-time scanning.</p>\n<p><strong>Best for:</strong> Teams that need ongoing monitoring rather than point-in-time scans. Worth evaluating if continuous security visibility is a priority.</p>\n<p><strong>Pricing:</strong> Check their website for current pricing.</p>\n<h3>SupaRalph (Open Source)</h3>\n<p>SupaRalph is an open-source tool available on GitHub that takes an active testing approach to Supabase security. Instead of passively analyzing configuration, it runs actual exploit tests against your project.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>Active exploit testing</li>\n<li>RLS bypass attempts</li>\n<li>Security boundary validation</li>\n</ul>\n<p><strong>How it works:</strong> Runs automated exploit attempts against your Supabase project to find real vulnerabilities. This is closer to penetration testing than configuration scanning.</p>\n<p><strong>Best for:</strong> Developers comfortable with CLI tools who want to know if their project can actually be exploited, not just whether the configuration looks correct. The active testing approach catches issues that passive scanners miss.</p>\n<p><strong>Pricing:</strong> Free and open source.</p>\n<h3>Supabase RLS Scanner (Firefox Extension)</h3>\n<p>A Firefox browser extension focused specifically on RLS policy checking. Lightweight and single-purpose.</p>\n<p><strong>What it covers:</strong></p>\n<ul>\n<li>RLS policy scanning via browser</li>\n</ul>\n<p><strong>How it works:</strong> Browser extension that checks RLS policies on Supabase projects you are working with in the Supabase dashboard.</p>\n<p><strong>Best for:</strong> Firefox users who want a quick RLS check without leaving the browser. Limited scope but zero friction.</p>\n<p><strong>Pricing:</strong> Free browser extension.</p>\n<h2>Feature Comparison Matrix</h2>\n<table>\n<thead>\n<tr>\n<th>Feature</th>\n<th align=\"center\">SupaSecScan</th>\n<th align=\"center\">VibeAppScanner</th>\n<th align=\"center\">AuditYourApp</th>\n<th align=\"center\">SupaExplorer</th>\n<th align=\"center\">Supaguard</th>\n<th align=\"center\">SupaRalph</th>\n<th align=\"center\">RLS Scanner (FF)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>RLS scanning</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n</tr>\n<tr>\n<td>Storage checks</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>Auth config audit</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>API key detection</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>CI/CD integration</td>\n<td align=\"center\">Yes (GitHub Action)</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>CLI tool</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">--</td>\n<td align=\"center\">--</td>\n<td align=\"center\">No</td>\n<td align=\"center\">--</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>Web interface</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>Browser extension</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Chrome</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Firefox</td>\n</tr>\n<tr>\n<td>Multi-platform</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Yes (Firebase)</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>Active exploit testing</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">No</td>\n</tr>\n<tr>\n<td>Open source</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">No</td>\n<td align=\"center\">Yes</td>\n<td align=\"center\">Yes</td>\n</tr>\n</tbody></table>\n<p><em>&quot;--&quot; indicates the feature was not confirmed at time of writing. Check each tool&#39;s current documentation for the latest capabilities.</em></p>\n<h2>Which Tool Should You Use?</h2>\n<p>The right choice depends on your team size, workflow, and what keeps you up at night.</p>\n<h3>Solo developer or freelancer</h3>\n<p>Start with <strong>SupaSecScan</strong> for a comprehensive one-time scan. If you are on Firefox, the <strong>RLS Scanner extension</strong> gives you a quick sanity check while developing. If you want to go deeper, run <strong>SupaRalph</strong> to see if your project is actually exploitable.</p>\n<h3>Startup team (2-10 developers)</h3>\n<p><strong>SupaSecScan</strong> with the GitHub Action gives you automated security checks on every PR. Pair it with <strong>SupaExplorer</strong> to verify your deployed frontend is not leaking keys. As your <a href=\"/blog/supabase-security-checklist-production\">security checklist</a> grows, having CI/CD integration becomes non-negotiable.</p>\n<h3>Enterprise or agency</h3>\n<p>Start with <strong>SupaSecScan</strong> for full-stack coverage and CI/CD integration. If you manage Firebase projects alongside Supabase, add <strong>AuditYourApp</strong> for cross-platform coverage. Consider <strong>Supaguard</strong> for continuous monitoring. Run <strong>SupaRalph</strong> periodically for active exploit validation.</p>\n<h2>The Bigger Picture</h2>\n<p>Tools are only part of the equation. The <a href=\"/blog/state-of-supabase-security-2026\">state of Supabase security in 2026</a> shows that most vulnerabilities come from developers not knowing what to check -- not from sophisticated attacks. A scanner catches the gaps. But understanding <a href=\"/blog/how-to-audit-supabase-rls-policies\">why RLS matters</a> and <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">what mistakes are most common</a> is what prevents them in the first place.</p>\n<p>If you are building with AI code generators, the risk is amplified. <a href=\"/blog/vibe-coding-automated-security-scanning\">Vibe coding tools skip security by default</a>, which means automated scanning is not optional -- it is your safety net.</p>\n<h2>Frequently Asked Questions</h2>\n<p><strong>Do I need a security scanner if I wrote my RLS policies myself?</strong></p>\n<p>Yes. Manual RLS policies are better than none, but they are easy to get wrong. A scanner catches missing policies on new tables, overly permissive SELECT rules, and gaps you did not think to test.</p>\n<p><strong>Can I use multiple tools together?</strong></p>\n<p>Absolutely. Many teams run a full-stack scanner like SupaSecScan in CI/CD and use SupaExplorer for quick deployed-app checks. The tools are complementary, not competitive.</p>\n<p><strong>Are these tools safe to run against my production database?</strong></p>\n<p>The scanners listed here (except SupaRalph) run non-destructive, read-only checks. SupaRalph performs active exploit tests -- run it against a staging environment first.</p>\n<p><strong>How often should I scan?</strong></p>\n<p>Every PR at minimum. Your security posture changes with every schema migration, every new table, and every RLS policy update. Continuous scanning catches drift.</p>",
  },
  {
    slug: "how-to-find-leaked-supabase-api-keys",
    title: "How to Find Leaked Supabase API Keys in Your Frontend",
    description:
      "Step-by-step guide to finding leaked Supabase service role keys in your frontend code. Manual checks, automated tools, and what to do if your key is exposed.",
    category: "Guides",
    publishedAt: "2026-04-15",
    keywords: ["supabase api key leaked","supabase anon key exposed","find leaked supabase keys","supabase service role key"],
    faqs: [
      { question: "Is it safe to expose my Supabase anon key?", answer: "Yes, if your RLS policies are correctly configured. The anon key is designed to be public. It respects your Row Level Security rules, so it can only access data you have explicitly allowed. Check your [RLS policies](/blog/how-to-audit-supabase-rls-policies) to make sure they are actually enforcing the right rules." },
      { question: "How do I know if someone already used my leaked key?", answer: "Check your Supabase dashboard logs and the Postgres logs for unusual query patterns. Look for queries against tables that your application does not normally access, bulk SELECT operations, or auth admin operations." },
      { question: "Can I use the service role key in a Next.js Server Component?", answer: "Yes. Server Components run exclusively on the server -- their code is never sent to the client. Use the service role key in Server Components, API routes, and Server Actions. Just make sure the environment variable does not have the `NEXT_PUBLIC_` prefix." },
      { question: "What about edge functions and middleware?", answer: "Edge functions and middleware run on the server. The service role key is safe to use there, as long as it is stored in server-side environment variables and not passed to the client response." },
    ],
    body: "<p>Every Supabase project ships with two API keys: an anon key and a service role key. The anon key is designed to be public -- it lives in your frontend code and respects your RLS policies. The service role key bypasses all security. If that key ends up in your client bundle, anyone with browser DevTools can read every row in every table, delete your storage files, and modify your auth configuration.</p>\n<p>This is not a theoretical risk. Leaked service role keys are one of the most common Supabase vulnerabilities in the wild, and they are trivially easy to introduce by accident.</p>\n<h2>How Supabase Keys Work</h2>\n<p>Supabase uses two JWT tokens to authenticate API requests:</p>\n<p><strong>Anon key (public):</strong> This key is meant to be in your frontend. Every request made with this key is filtered through your Row Level Security policies. If your RLS is configured correctly, this key cannot access data it should not see. It is safe to expose -- that is its entire purpose.</p>\n<p><strong>Service role key (secret):</strong> This key bypasses RLS entirely. It has full read/write access to every table, every storage bucket, and every auth record. It should never leave your server. Ever.</p>\n<p>The problem: both keys look similar. Both are long JWT strings. Both start with <code>eyJ</code>. In a <code>.env</code> file, they sit right next to each other. One copy-paste mistake, one wrong environment variable reference, and your service role key is in your production bundle.</p>\n<h2>How to Check If Your Keys Are Exposed</h2>\n<h3>Step 1: Inspect Your Environment Variables</h3>\n<p>Check your <code>.env.local</code> or <code>.env</code> file. You should see something like this:</p>\n<pre><code>NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co\nNEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...\nSUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...\n</code></pre>\n<p>The critical detail: any environment variable prefixed with <code>NEXT_PUBLIC_</code> (Next.js), <code>VITE_</code> (Vite), or <code>REACT_APP_</code> (Create React App) is bundled into your client-side code. If your service role key uses one of these prefixes, it is exposed.</p>\n<p><strong>Check for:</strong> <code>NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY</code>, <code>VITE_SUPABASE_SERVICE_ROLE_KEY</code>, or any variation where the service role key has a public prefix.</p>\n<h3>Step 2: Search Your Source Code</h3>\n<p>Search your codebase for direct references to the service role key:</p>\n<pre><code class=\"language-bash\">grep -r &quot;service_role&quot; --include=&quot;*.ts&quot; --include=&quot;*.tsx&quot; --include=&quot;*.js&quot; --include=&quot;*.jsx&quot; src/\n</code></pre>\n<p>Look for patterns like:</p>\n<ul>\n<li><code>createClient(url, process.env.NEXT_PUBLIC_SERVICE_ROLE_KEY)</code></li>\n<li>Hard-coded JWT strings in client components</li>\n<li>Supabase admin client initialized in files that are not server-only</li>\n</ul>\n<p>Any Supabase client created with the service role key must exist exclusively in server-side code: API routes, server actions, server components, or backend services.</p>\n<h3>Step 3: Check Your Production Bundle</h3>\n<p>Deploy your app (or build it locally) and inspect the output:</p>\n<pre><code class=\"language-bash\">npm run build\ngrep -r &quot;service_role&quot; .next/static/ 2&gt;/dev/null\ngrep -r &quot;service_role&quot; dist/ 2&gt;/dev/null\n</code></pre>\n<p>For a more thorough check, open your deployed app in a browser and use DevTools:</p>\n<ol>\n<li>Open <strong>Sources</strong> tab</li>\n<li>Search across all files (Ctrl+Shift+F / Cmd+Shift+F)</li>\n<li>Search for <code>service_role</code> or your known service role key prefix</li>\n<li>Also search for <code>supabase</code> to find all Supabase client initializations</li>\n</ol>\n<p>If your service role key appears anywhere in the client bundle, it is compromised. Rotate it immediately.</p>\n<h3>Step 4: Use Automated Detection</h3>\n<p>Manual checks catch obvious leaks, but automated tools catch the subtle ones -- environment variable indirection, build-time inlining, and keys passed through props or context.</p>\n<p><strong>SupaSecScan</strong> checks for API key exposure as part of its full security scan. Point it at your project and it flags any keys that should not be public. This is faster and more reliable than manual grep, especially in larger codebases.</p>\n<p>Browser extensions like <strong>SupaExplorer</strong> can scan deployed applications for exposed keys from the outside -- useful for checking production without access to the source code.</p>\n<h2>What to Do If Your Service Role Key Is Exposed</h2>\n<p>Act immediately. A leaked service role key gives full database access to anyone who finds it.</p>\n<h3>1. Rotate the key</h3>\n<p>Go to your Supabase dashboard &gt; Settings &gt; API. Click &quot;Regenerate&quot; on the service role key. The old key is invalidated instantly.</p>\n<h3>2. Update your server-side references</h3>\n<p>Replace the old key in your server environment:</p>\n<ul>\n<li>Update <code>.env</code> files (not <code>.env.local</code> on the client side)</li>\n<li>Update your hosting platform&#39;s environment variables (Vercel, Netlify, Railway, etc.)</li>\n<li>Update any CI/CD secrets that reference the key</li>\n<li>Restart your server processes to pick up the new key</li>\n</ul>\n<h3>3. Audit for damage</h3>\n<p>Check your Supabase logs for unusual activity during the exposure window. Look for:</p>\n<ul>\n<li>Unexpected data reads across multiple tables</li>\n<li>Bulk data exports</li>\n<li>Auth user modifications</li>\n<li>Storage file access or deletion</li>\n</ul>\n<h3>4. Fix the root cause</h3>\n<p>Determine how the key leaked and fix the configuration. Common causes:</p>\n<ul>\n<li>Wrong environment variable prefix</li>\n<li>Service role key in a shared Supabase client used by both server and client code</li>\n<li>Copy-paste error in <code>.env</code> setup</li>\n</ul>\n<h2>Prevention: Keep Your Keys Where They Belong</h2>\n<h3>Use framework-specific conventions</h3>\n<p>Every modern framework separates public and private environment variables. Use the conventions:</p>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Public prefix</th>\n<th>Private (server-only)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Next.js</td>\n<td><code>NEXT_PUBLIC_</code></td>\n<td>No prefix</td>\n</tr>\n<tr>\n<td>Vite</td>\n<td><code>VITE_</code></td>\n<td>No prefix</td>\n</tr>\n<tr>\n<td>Create React App</td>\n<td><code>REACT_APP_</code></td>\n<td>Not supported natively</td>\n</tr>\n<tr>\n<td>SvelteKit</td>\n<td><code>PUBLIC_</code></td>\n<td>No prefix</td>\n</tr>\n<tr>\n<td>Nuxt</td>\n<td><code>NUXT_PUBLIC_</code></td>\n<td>No prefix</td>\n</tr>\n</tbody></table>\n<p>Your service role key must never have a public prefix. Name it <code>SUPABASE_SERVICE_ROLE_KEY</code> and access it only in server-side code.</p>\n<h3>Separate your Supabase clients</h3>\n<p>Create two distinct Supabase client files:</p>\n<ul>\n<li><code>lib/supabase/client.ts</code> -- uses the anon key, imported by client components</li>\n<li><code>lib/supabase/server.ts</code> -- uses the service role key, imported only by server code</li>\n</ul>\n<p>Never create a single &quot;universal&quot; Supabase client that switches keys based on context. The separation should be structural, not conditional.</p>\n<h3>Add build-time checks</h3>\n<p>Add a build step or lint rule that greps your output bundle for the service role key pattern. If it appears, fail the build. This is your last line of defense before deployment.</p>\n<h3>Use a security scanner in CI/CD</h3>\n<p>Integrate <a href=\"/blog/vibe-coding-automated-security-scanning\">automated security scanning</a> into your CI/CD pipeline. SupaSecScan&#39;s GitHub Action checks for key exposure on every pull request, catching leaks before they reach production.</p>\n<h2>The Vibe Coding Problem</h2>\n<p>If you used an AI code generator to build your Supabase project, check your keys carefully. AI tools frequently generate environment variable configurations that put the service role key behind a public prefix. The AI does not know the difference between your anon key and your service role key -- it just sees two environment variables and makes them both accessible.</p>\n<p>This is one of the most dangerous <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">vibe coding security gaps</a>: the code works perfectly, passes every functional test, and ships with a catastrophic vulnerability.</p>\n<h2>Frequently Asked Questions</h2>\n<p><strong>Is it safe to expose my Supabase anon key?</strong></p>\n<p>Yes, if your RLS policies are correctly configured. The anon key is designed to be public. It respects your Row Level Security rules, so it can only access data you have explicitly allowed. Check your <a href=\"/blog/how-to-audit-supabase-rls-policies\">RLS policies</a> to make sure they are actually enforcing the right rules.</p>\n<p><strong>How do I know if someone already used my leaked key?</strong></p>\n<p>Check your Supabase dashboard logs and the Postgres logs for unusual query patterns. Look for queries against tables that your application does not normally access, bulk SELECT operations, or auth admin operations.</p>\n<p><strong>Can I use the service role key in a Next.js Server Component?</strong></p>\n<p>Yes. Server Components run exclusively on the server -- their code is never sent to the client. Use the service role key in Server Components, API routes, and Server Actions. Just make sure the environment variable does not have the <code>NEXT_PUBLIC_</code> prefix.</p>\n<p><strong>What about edge functions and middleware?</strong></p>\n<p>Edge functions and middleware run on the server. The service role key is safe to use there, as long as it is stored in server-side environment variables and not passed to the client response.</p>",
  },
  {
    slug: "vibe-coders-supabase-rls-security",
    title: "Supabase Security for Vibe Coders: Why AI-Generated Code Skips RLS",
    description:
      "AI code generators ship Supabase apps without RLS policies. Learn why Cursor, v0, Bolt, and Lovable skip row-level security and how to fix it in minutes.",
    category: "Vibe Coding",
    publishedAt: "2026-04-15",
    keywords: ["vibe coding supabase security","ai generated code supabase rls","supabase rls","vibe coding security"],
    faqs: [
      { question: "Does Supabase enable RLS by default?", answer: "No. New tables are created with RLS disabled. You must explicitly enable it with `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` and then create policies. This is a deliberate design choice -- Supabase does not want to break new projects with unexpected access restrictions. But it means every new table starts wide open." },
      { question: "Can I prompt the AI to add RLS?", answer: "Yes, and you should. Adding \"enable RLS and add appropriate policies for each table\" to your prompts improves the output significantly. But verify the result -- AI-generated policies are often too permissive (`USING (true)`) or structurally wrong." },
      { question: "What if my app breaks after enabling RLS?", answer: "This usually means your frontend is querying data without an authenticated user session, or your policies are too restrictive. Check that you are passing the user's JWT with every Supabase client request and that your policies match your app's actual access patterns." },
      { question: "Is RLS enough to secure my Supabase project?", answer: "RLS is the most critical layer, but not the only one. You also need to check [storage bucket policies](/blog/supabase-storage-buckets-probably-public), auth configuration, and API key handling. A comprehensive scanner covers all of these in one pass." },
    ],
    body: "<p>You described your app in a prompt. The AI generated a full-stack Supabase project in minutes. It works. Users can sign up, create data, and interact with your app. Ship it.</p>\n<p>Except the AI did not add Row Level Security policies to your tables. Every row in your database is readable by every user. Anyone with your anon key -- which is in your frontend code by design -- can query any table and get back everything.</p>\n<p>This is not a bug in the AI. It is a structural gap in how AI code generators handle security, and it affects virtually every vibe-coded Supabase project.</p>\n<h2>The Vibe Coding Boom</h2>\n<p>Tools like Cursor, v0, Bolt, and Lovable have changed how developers build. Describe what you want, get working code. The speed is real: a functional Supabase-backed app in an afternoon instead of a week.</p>\n<p>But speed and security pull in opposite directions. The AI optimizes for &quot;it works&quot; -- meaning the app compiles, runs, and produces the expected output. Security is invisible when the app works correctly. <a href=\"/blog/how-to-audit-supabase-rls-policies\">RLS policies</a> do not change how the app behaves for legitimate users. They only matter when someone tries to access data they should not see.</p>\n<p>The AI does not test for that. Neither do most developers during a vibe coding session.</p>\n<h2>Why AI Skips Security</h2>\n<p>There are three reasons AI code generators consistently produce Supabase code without proper RLS:</p>\n<h3>1. Training data bias</h3>\n<p>Most Supabase tutorials, documentation examples, and Stack Overflow answers show table creation without RLS. The quickstart guides focus on getting data flowing. Security is a separate topic, covered in separate docs, linked from separate pages. The AI learned from this separation: first make it work, then (maybe) make it secure.</p>\n<p>When you prompt &quot;create a Supabase table for user profiles,&quot; the AI generates:</p>\n<pre><code class=\"language-sql\">CREATE TABLE profiles (\n  id UUID REFERENCES auth.users PRIMARY KEY,\n  username TEXT,\n  avatar_url TEXT,\n  created_at TIMESTAMPTZ DEFAULT NOW()\n);\n</code></pre>\n<p>Clean. Correct. No RLS. The table is readable and writable by anyone with your anon key.</p>\n<h3>2. RLS adds complexity the AI avoids</h3>\n<p>Correct RLS policies require understanding the application&#39;s authorization model: who can read what, who can write what, under which conditions. This is context the AI often does not have.</p>\n<p>A proper RLS setup for that same profiles table looks like:</p>\n<pre><code class=\"language-sql\">ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;\n\nCREATE POLICY &quot;Users can view their own profile&quot;\n  ON profiles FOR SELECT\n  USING (auth.uid() = id);\n\nCREATE POLICY &quot;Users can update their own profile&quot;\n  ON profiles FOR UPDATE\n  USING (auth.uid() = id);\n\nCREATE POLICY &quot;Users can insert their own profile&quot;\n  ON profiles FOR INSERT\n  WITH CHECK (auth.uid() = id);\n</code></pre>\n<p>That is three policies for one table. A typical app has 5-15 tables. The AI would need to understand the relationships between all of them to generate correct policies. Most of the time, it does not try.</p>\n<h3>3. Convenience over safety</h3>\n<p>AI code generators are optimized for developer satisfaction. Code that works immediately feels good. Code that fails because of a missing RLS policy requires debugging -- and debugging RLS is notoriously confusing. Queries return empty results instead of errors. The AI knows this and takes the path of least resistance: skip RLS, and the app works for everyone.</p>\n<h2>Real Examples: What AI Gets Wrong</h2>\n<p>Here are patterns that appear consistently in AI-generated Supabase code:</p>\n<p><strong>Tables without RLS enabled.</strong> The most basic issue. <code>ALTER TABLE ... ENABLE ROW LEVEL SECURITY</code> is simply never called. Every table is wide open.</p>\n<p><strong>RLS enabled but no policies.</strong> Some AI tools have learned to enable RLS (maybe from prompt templates). But enabling RLS without adding policies locks out everyone -- including your app. The AI sees the app break, disables RLS, and moves on.</p>\n<p><strong>Overly permissive policies.</strong> When the AI does generate policies, they tend toward <code>USING (true)</code> -- which allows everyone to do everything. This is technically RLS, but it enforces nothing.</p>\n<p><strong>Service role key in the client.</strong> The AI creates a Supabase client using the service role key (which bypasses RLS entirely) because it &quot;just works&quot; without needing to think about policies. This is the equivalent of giving every user admin access. See our guide on <a href=\"/blog/how-to-find-leaked-supabase-api-keys\">finding leaked API keys</a> for why this is catastrophic.</p>\n<p><strong>No storage policies.</strong> Even when table RLS is addressed, <a href=\"/blog/supabase-storage-buckets-probably-public\">storage bucket policies</a> are almost always missing. The AI creates a public bucket because uploads work immediately.</p>\n<h2>The Five-Minute Fix</h2>\n<p>You do not need to rewrite your app. You need to add RLS policies to your existing tables. Here is how to do it in five minutes:</p>\n<h3>1. List your tables</h3>\n<p>Open the Supabase SQL Editor and run:</p>\n<pre><code class=\"language-sql\">SELECT tablename, rowsecurity\nFROM pg_tables\nWHERE schemaname = &#39;public&#39;;\n</code></pre>\n<p>Any table where <code>rowsecurity</code> is <code>false</code> is exposed.</p>\n<h3>2. Enable RLS on every table</h3>\n<pre><code class=\"language-sql\">ALTER TABLE your_table_name ENABLE ROW LEVEL SECURITY;\n</code></pre>\n<p>Do this for every public table. No exceptions.</p>\n<h3>3. Add basic policies</h3>\n<p>For user-owned data (most common pattern in vibe-coded apps):</p>\n<pre><code class=\"language-sql\">CREATE POLICY &quot;Users can read own data&quot;\n  ON your_table_name FOR SELECT\n  USING (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users can insert own data&quot;\n  ON your_table_name FOR INSERT\n  WITH CHECK (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users can update own data&quot;\n  ON your_table_name FOR UPDATE\n  USING (auth.uid() = user_id);\n\nCREATE POLICY &quot;Users can delete own data&quot;\n  ON your_table_name FOR DELETE\n  USING (auth.uid() = user_id);\n</code></pre>\n<p>Replace <code>user_id</code> with whatever column references <code>auth.users.id</code> in your table.</p>\n<h3>4. Test it</h3>\n<p>Log in as a test user. Verify they can only see their own data. Try querying other users&#39; data -- it should return empty results, not errors.</p>\n<h3>5. Handle shared data</h3>\n<p>Some tables are intentionally public (blog posts, product listings). Create appropriate read policies:</p>\n<pre><code class=\"language-sql\">CREATE POLICY &quot;Anyone can read published posts&quot;\n  ON posts FOR SELECT\n  USING (published = true);\n</code></pre>\n<p>The key: every table should have explicit policies. No table should rely on &quot;RLS is disabled&quot; as its access model.</p>\n<h2>Automated Scanning: Catch What AI Missed</h2>\n<p>Manual RLS audits work for small projects. But as your app grows -- and as you continue using AI to add features -- new tables appear without policies. Columns change. Relationships shift.</p>\n<p><a href=\"/blog/vibe-coding-automated-security-scanning\">Automated security scanning</a> catches these gaps continuously. SupaSecScan checks every table for RLS status, evaluates your policies for common <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">mistakes</a>, and flags new tables that were added without security.</p>\n<p>Integrating a scanner into your workflow means every AI-generated migration gets checked before it reaches production. The AI writes the code. The scanner verifies it is safe. You ship with confidence.</p>\n<h2>The Uncomfortable Truth</h2>\n<p>AI code generators are extraordinary tools. They compress weeks of work into hours. But they have a blind spot for security that maps exactly onto the most dangerous class of Supabase vulnerabilities.</p>\n<p>The <a href=\"/blog/state-of-supabase-security-2026\">state of Supabase security in 2026</a> tells the story: most compromised projects were built quickly, tested for functionality, and deployed without a security review. Vibe coding accelerates every step of that process -- including the part where security gets skipped.</p>\n<p>The fix is not to stop using AI. The fix is to add a security check between &quot;it works&quot; and &quot;it ships.&quot; A five-minute RLS review. An automated scanner in your CI pipeline. A <a href=\"/blog/supabase-security-checklist-production\">security checklist</a> pinned to your project board.</p>\n<p>The AI built your app. Now make sure it is actually safe.</p>\n<h2>Frequently Asked Questions</h2>\n<p><strong>Does Supabase enable RLS by default?</strong></p>\n<p>No. New tables are created with RLS disabled. You must explicitly enable it with <code>ALTER TABLE ... ENABLE ROW LEVEL SECURITY</code> and then create policies. This is a deliberate design choice -- Supabase does not want to break new projects with unexpected access restrictions. But it means every new table starts wide open.</p>\n<p><strong>Can I prompt the AI to add RLS?</strong></p>\n<p>Yes, and you should. Adding &quot;enable RLS and add appropriate policies for each table&quot; to your prompts improves the output significantly. But verify the result -- AI-generated policies are often too permissive (<code>USING (true)</code>) or structurally wrong.</p>\n<p><strong>What if my app breaks after enabling RLS?</strong></p>\n<p>This usually means your frontend is querying data without an authenticated user session, or your policies are too restrictive. Check that you are passing the user&#39;s JWT with every Supabase client request and that your policies match your app&#39;s actual access patterns.</p>\n<p><strong>Is RLS enough to secure my Supabase project?</strong></p>\n<p>RLS is the most critical layer, but not the only one. You also need to check <a href=\"/blog/supabase-storage-buckets-probably-public\">storage bucket policies</a>, auth configuration, and API key handling. A comprehensive scanner covers all of these in one pass.</p>",
  },
];

export function getAllPosts(): readonly BlogPost[] {
  return posts.toSorted(
    (a, b) =>
      new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime(),
  );
}

export function getPostBySlug(slug: string): BlogPost | undefined {
  return posts.find((p) => p.slug === slug);
}

export function getPostsByCategory(
  category: string,
): readonly BlogPost[] {
  return posts
    .filter((p) => p.category === category)
    .toSorted(
      (a, b) =>
        new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime(),
    );
}

export function getAllSlugs(): readonly string[] {
  return posts.map((p) => p.slug);
}
