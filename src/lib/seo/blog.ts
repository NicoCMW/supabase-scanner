import type { BlogPost } from "./types";

const posts: readonly BlogPost[] = [
  {
    slug: "state-of-supabase-security-2026",
    title: "The State of Supabase Security in 2026",
    description:
      "How secure are Supabase applications in 2026? We analyzed common vulnerability patterns across Supabase projects and found that most apps share the same critical misconfigurations.",
    category: "Security Basics",
    publishedAt: "2026-03-15",
    keywords: [
      "supabase security",
      "supabase security audit",
      "is supabase secure",
      "supabase RLS",
    ],
    body: "<p>Supabase has become the default database for a generation of developers. It powers side projects, startups, and increasingly, production SaaS applications serving real users with real data. Its developer experience is unmatched: spin up a Postgres database, get instant REST and GraphQL APIs, add authentication in minutes, and deploy.</p>\n\n<p>But there is a gap between how easy Supabase makes it to build and how much security knowledge it assumes you have. And that gap is widening.</p>\n\n<p>In 2026, three forces are converging to make Supabase security a critical topic:</p>\n\n<ol>\n<li><strong>Supabase adoption is accelerating.</strong> The platform has grown from a developer tool into infrastructure that runs real businesses.</li>\n<li><strong>Vibe coding has gone mainstream.</strong> Developers are shipping AI-generated code faster than ever, often without reviewing the security implications.</li>\n<li><strong>Attackers have noticed.</strong> The same API patterns that make Supabase great for developers make misconfigured projects easy to discover and exploit.</li>\n</ol>\n\n<p>This article examines the current state of Supabase security: what is going well, what is going wrong, and what every developer using Supabase should know heading into the rest of 2026.</p>\n\n<h3>The Security Model: Powerful but Misunderstood</h3>\n\n<p>Supabase's security model is built on three pillars:</p>\n\n<p><strong>Row Level Security (RLS):</strong> Postgres-native policies that control which rows each user can read, write, update, or delete. RLS is the primary access control mechanism for Supabase's auto-generated APIs. If you are unfamiliar with common RLS pitfalls, read our guide on <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<p><strong>API Key Separation:</strong> Two keys with fundamentally different privilege levels. The <code>anon</code> key respects RLS policies. The <code>service_role</code> key bypasses them entirely.</p>\n\n<p><strong>Authentication:</strong> Built-in auth with email/password, OAuth providers, magic links, and phone authentication. JWTs carry user claims that RLS policies evaluate.</p>\n\n<p>This is a strong foundation. Supabase's approach of leveraging Postgres's built-in security features rather than inventing a proprietary system means you get battle-tested access control backed by decades of Postgres development.</p>\n\n<p>But here is the problem: the model only works when developers configure it correctly. And the evidence suggests that many do not.</p>\n\n<h3>The RLS Problem</h3>\n\n<p>Row Level Security is Supabase's most important security feature and its most commonly misconfigured one.</p>\n\n<p>The core issue is straightforward. When you create a new table in Supabase, RLS is now enabled by default (a welcome change made in late 2025). But enabling RLS is only half the equation. Without policies defining who can access what, an RLS-enabled table with no policies is completely inaccessible -- which means developers quickly learn to either disable RLS temporarily or write overly permissive policies to get their application working.</p>\n\n<p>Both paths lead to vulnerabilities.</p>\n\n<p><strong>Pattern 1: RLS disabled entirely.</strong> The developer disables RLS during development and forgets to re-enable it before deploying. Every row in the table is accessible to anyone with the project URL and anon key -- both of which are embedded in frontend code and trivially extractable.</p>\n\n<p><strong>Pattern 2: Overly permissive policies.</strong> The developer writes a policy like <code>CREATE POLICY \"allow all\" ON public.users FOR ALL USING (true)</code> because it makes the application work. This is functionally equivalent to having no RLS at all.</p>\n\n<p><strong>Pattern 3: Incomplete policy coverage.</strong> The developer writes a SELECT policy but forgets that UPDATE and DELETE operations need their own policies. Or they write a policy using <code>USING</code> but forget that INSERT operations require <code>WITH CHECK</code>.</p>\n\n<p>These are not hypothetical scenarios. They are the most common patterns found in real Supabase applications. For a detailed guide on fixing these issues, see <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>. We break down each of these mistakes in detail in <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a>.</p>\n\n<h3>The Vibe Coding Amplifier</h3>\n\n<p>The rise of vibe coding -- prompt-driven development where AI generates the code -- has amplified these security patterns. When a developer tells an AI assistant to add a comments table to their Supabase project, the generated code typically creates the table, sets up the schema, and writes the client-side queries. What it often does not do:</p>\n\n<ul>\n<li>Enable RLS with appropriate policies</li>\n<li>Validate that the anon key is not being used for operations that require the service role</li>\n<li>Check whether storage buckets are set to public or private</li>\n<li>Implement proper input validation before database operations</li>\n</ul>\n\n<p>Research from Veracode found that nearly half of AI-generated code contains known vulnerabilities. The Moltbook incident in February 2026 -- where a social networking site built entirely through vibe coding exposed 1.5 million authentication tokens -- demonstrated what happens when this code reaches production.</p>\n\n<p>The issue is not that AI generates bad code. The issue is that AI generates code optimized for functionality, not security. And most developers accepting that code lack the security expertise to identify what is missing.</p>\n\n<h3>Storage: The Overlooked Attack Surface</h3>\n\n<p>While RLS gets most of the security attention, Supabase Storage is quietly becoming a significant attack surface. We wrote a deep-dive on this topic: <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>. Many applications store sensitive files -- user uploads, documents, profile photos with metadata -- in public buckets because it is simpler.</p>\n\n<p>Public bucket URLs follow a predictable pattern, making enumeration trivial. We cover this in depth in <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>, including how to check your buckets and migrate to secure access patterns.</p>\n\n<h3>API Key Exposure</h3>\n\n<p>Every Supabase project has two API keys that serve fundamentally different purposes:</p>\n\n<ul>\n<li><strong><code>anon</code> key:</strong> Safe to use in frontend code. All requests are filtered through RLS policies.</li>\n<li><strong><code>service_role</code> key:</strong> Bypasses all RLS. Intended exclusively for server-side use.</li>\n</ul>\n\n<p>The most dangerous misconfiguration is using the <code>service_role</code> key in client-side code. This gives every user of your application full, unrestricted access to your entire database. No RLS policies apply. No row-level restrictions matter.</p>\n\n<p>This happens more often than you might expect, particularly in AI-generated code where the model reaches for the key that works without distinguishing between frontend and backend contexts.</p>\n\n<h3>What Supabase Is Doing Right</h3>\n\n<p>It is worth noting that Supabase has been making significant security improvements:</p>\n\n<ul>\n<li><strong>RLS enabled by default</strong> on new tables (late 2025)</li>\n<li><strong>Security Advisor</strong> in the dashboard that flags common misconfigurations</li>\n<li><strong>Column-level security</strong> adding another layer of data protection</li>\n<li><strong>Option to disable the Data API</strong> entirely for projects that do not need auto-generated endpoints</li>\n<li><strong>SOC 2 Type 2 compliance</strong> with regular third-party audits</li>\n<li><strong>Active HackerOne program</strong> for vulnerability disclosure</li>\n<li><strong>Push protection and grant toggles</strong> on the 2026 roadmap</li>\n</ul>\n\n<p>These are meaningful improvements. But they address the platform side of security. The developer side -- how applications are built on top of Supabase -- remains the primary source of vulnerabilities.</p>\n\n<h3>What You Should Do Today</h3>\n\n<p>If you are building on Supabase, here is your minimum security checklist:</p>\n\n<ol>\n<li><strong>Audit every table's RLS status.</strong> Run <code>SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname = 'public';</code> in the SQL Editor. Every table should show <code>true</code>.</li>\n<li><strong>Review your RLS policies.</strong> For each table, verify that policies exist for every operation your application performs. Pay special attention to UPDATE and DELETE policies.</li>\n<li><strong>Check your API key usage.</strong> Search your frontend codebase for your <code>service_role</code> key. If you find it, you have a critical vulnerability. Replace it with the <code>anon</code> key and add appropriate RLS policies.</li>\n<li><strong>Audit your storage buckets.</strong> Review whether each bucket needs to be public. If files contain user data, <a href=\"/blog/supabase-storage-buckets-probably-public\">switch to private buckets with signed URLs</a>.</li>\n<li><strong>Enable email verification</strong> if you use email/password auth. Without it, anyone can create accounts with unverified email addresses.</li>\n<li><strong>Run the Supabase Security Advisor</strong> in your project dashboard. Address every finding.</li>\n<li><strong>Automate your security scanning.</strong> Manual audits are point-in-time. <a href=\"/login\">Run a free scan of your Supabase project</a> to catch misconfigurations as your codebase evolves.</li>\n</ol>\n\n<h3>Looking Ahead</h3>\n\n<p>Supabase security is not a solved problem. The platform provides strong primitives, but the gap between available security features and correctly configured security is where vulnerabilities live.</p>\n\n<p>As Supabase adoption grows and vibe coding becomes the default development workflow, the need for automated security validation will only increase. The developers building on Supabase are not security experts, and they should not have to be. But they do need tools that catch the misconfigurations before attackers do.</p>\n\n<p>The state of Supabase security in 2026 is this: the platform is more secure than ever. The applications built on it, on average, are not.</p>\n\n<p>That is the gap we need to close.</p>\n\n<p><strong><a href=\"/login\">Scan your Supabase project for free --></a></strong></p>",
  },
  {
    slug: "5-rls-mistakes-every-vibe-coder-makes",
    title: "5 RLS Mistakes Every Vibe Coder Makes",
    description:
      "Row Level Security is your last line of defense in Supabase. These are the five most common RLS mistakes we see in AI-generated code and how to fix each one.",
    category: "RLS",
    publishedAt: "2026-03-18",
    keywords: [
      "supabase RLS",
      "supabase RLS mistakes",
      "row level security",
      "vibe coding security",
    ],
    body: "<p>You asked your AI assistant to build a Supabase app. It generated the schema, wrote the queries, wired up the frontend. Everything works.</p>\n\n<p>But does it work <em>securely</em>?</p>\n\n<p>Row Level Security (RLS) is the mechanism that stands between your Supabase data and anyone with a browser. As we covered in <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>, RLS misconfiguration is the most common vulnerability pattern. When configured correctly, it ensures users only see and modify their own data. When misconfigured, it is the same as having no security at all.</p>\n\n<p>Here are the five RLS mistakes we see repeatedly in vibe-coded Supabase applications, and the exact SQL to fix each one. For broader context on the Supabase security landscape, see <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>.</p>\n\n<h3>Mistake 1: RLS Is Not Enabled</h3>\n\n<p><strong>What happened:</strong> Your AI assistant created tables using SQL migrations or the Supabase client, and RLS was never enabled. As of late 2025, Supabase enables RLS by default on new tables -- but if your project predates this change, or if your AI generated raw SQL that explicitly sets <code>ALTER TABLE ... DISABLE ROW LEVEL SECURITY</code>, your tables are wide open.</p>\n\n<p><strong>Why it matters:</strong> Without RLS, anyone with your project URL and <code>anon</code> key -- both of which are in your frontend JavaScript -- can read, write, update, and delete every row in the table. Your entire database is one <code>curl</code> command away from being dumped.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT schemaname, tablename, rowsecurity\nFROM pg_tables\nWHERE schemaname = 'public';</code></pre>\n\n<p>Every table should show <code>rowsecurity = true</code>.</p>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">ALTER TABLE public.your_table_name ENABLE ROW LEVEL SECURITY;\nALTER TABLE public.your_table_name FORCE ROW LEVEL SECURITY;</code></pre>\n\n<p>Run this for every table in your public schema. No exceptions.</p>\n\n<h3>Mistake 2: RLS Is Enabled but There Are No Policies</h3>\n\n<p><strong>What happened:</strong> RLS is enabled (good), but no policies were created (bad). This is the most confusing misconfiguration because your application appears broken -- all queries return empty results -- but there are no error messages.</p>\n\n<p><strong>Why it matters:</strong> The developer's instinct when faced with empty query results is to disable RLS. We have seen this pattern repeatedly: enable RLS, see empty results, panic, disable RLS, move on. The temporary fix becomes permanent.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT t.tablename\nFROM pg_tables t\nLEFT JOIN pg_policies p ON t.tablename = p.tablename AND t.schemaname = p.schemaname\nWHERE t.schemaname = 'public'\n  AND t.rowsecurity = true\nGROUP BY t.tablename\nHAVING COUNT(p.policyname) = 0;</code></pre>\n\n<p><strong>The fix:</strong> Add policies appropriate for your use case. Here is a common pattern for a user-owned data table:</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;Users can view own data&quot;\n  ON public.user_data FOR SELECT\n  USING (auth.uid() = user_id);\n\n<p>CREATE POLICY &quot;Users can insert own data&quot;   ON public.user_data FOR INSERT   WITH CHECK (auth.uid() = user_id);</p>\n\n<p>CREATE POLICY &quot;Users can update own data&quot;   ON public.user_data FOR UPDATE   USING (auth.uid() = user_id)   WITH CHECK (auth.uid() = user_id);</p>\n\n<p>CREATE POLICY &quot;Users can delete own data&quot;   ON public.user_data FOR DELETE   USING (auth.uid() = user_id);</code></pre></p>\n\n<h3>Mistake 3: Using <code>true</code> as the Policy Condition</h3>\n\n<p><strong>What happened:</strong> The developer (or AI) created a policy with <code>USING (true)</code> to quickly unblock development. This policy allows every authenticated (and sometimes unauthenticated) user to access every row.</p>\n\n<p><strong>Why it matters:</strong> A policy with <code>USING (true)</code> is functionally equivalent to not having RLS at all. It satisfies the RLS-is-enabled checkbox without actually providing any access control.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT schemaname, tablename, policyname, permissive, cmd, qual\nFROM pg_policies\nWHERE schemaname = 'public'\n  AND qual = 'true';</code></pre>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">DROP POLICY &quot;allow all&quot; ON public.your_table;\n\n<p>CREATE POLICY &quot;owner access only&quot;   ON public.your_table FOR ALL   USING (auth.uid() = user_id)   WITH CHECK (auth.uid() = user_id);</code></pre></p>\n\n<p>If some data genuinely needs to be public (e.g., published blog posts), use a policy that checks a status column:</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;public can read published posts&quot;\n  ON public.posts FOR SELECT\n  USING (status = 'published');</code></pre>\n\n<h3>Mistake 4: Forgetting WITH CHECK on INSERT and UPDATE</h3>\n\n<p><strong>What happened:</strong> The developer created a SELECT policy with <code>USING</code> but did not add <code>WITH CHECK</code> for INSERT or UPDATE operations. This is subtle because they serve different purposes:</p>\n\n<ul>\n<li><code>USING</code> filters which <em>existing</em> rows a user can see, update, or delete.</li>\n<li><code>WITH CHECK</code> validates what data a user can <em>write</em> during inserts and updates.</li>\n</ul>\n\n<p><strong>Why it matters:</strong> Without <code>WITH CHECK</code> on INSERT, there is no validation on new rows being created. A user could insert data with a different <code>user_id</code>, effectively writing data into another user's namespace.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT tablename, cmd, policyname,\n       qual IS NOT NULL AS has_using,\n       with_check IS NOT NULL AS has_with_check\nFROM pg_policies\nWHERE schemaname = 'public'\nORDER BY tablename, cmd;</code></pre>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;users can insert own data&quot;\n  ON public.profiles FOR INSERT\n  WITH CHECK (auth.uid() = user_id);\n\n<p>CREATE POLICY &quot;users can update own profile&quot;   ON public.profiles FOR UPDATE   USING (auth.uid() = user_id)   WITH CHECK (auth.uid() = user_id);</code></pre></p>\n\n<h3>Mistake 5: Not Indexing Columns Used in Policies</h3>\n\n<p><strong>What happened:</strong> RLS policies reference columns like <code>user_id</code> or <code>organization_id</code> in their <code>USING</code> and <code>WITH CHECK</code> clauses, but those columns have no indexes.</p>\n\n<p><strong>Why it matters:</strong> Every query against an RLS-protected table evaluates the policy conditions. Without indexes, this means a full table scan for every single query. On a table with 1 million rows, your API responses go from 50ms to 5 seconds. This causes developers to disable RLS for performance reasons, which creates a security vulnerability.</p>\n\n<p><strong>How to check:</strong></p>\n\n<pre><code class=\"language-sql\">SELECT tablename, policyname, qual, with_check\nFROM pg_policies WHERE schemaname = 'public';\n\n<p>SELECT indexname, tablename, indexdef FROM pg_indexes WHERE schemaname = 'public' AND tablename = 'your_table';</code></pre></p>\n\n<p><strong>The fix:</strong></p>\n\n<pre><code class=\"language-sql\">CREATE INDEX idx_user_data_user_id ON public.user_data (user_id);\nCREATE INDEX idx_documents_org_id ON public.documents (organization_id);</code></pre>\n\n<h3>The Bigger Picture</h3>\n\n<p>These five mistakes share a common root cause: RLS is a security system that looks like a database feature. Developers approach it with a make-it-work mindset rather than a make-it-secure mindset. AI code generators amplify this because they optimize for functionality -- the code compiles, the queries return data, the UI renders.</p>\n\n<p>Security is what the code <em>prevents</em>, not what it enables. Storage is another overlooked attack surface -- see <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a>. And that is exactly what gets lost in the vibe coding workflow.</p>\n\n<p>RLS is just one layer of your Supabase security posture. Your storage buckets are another common weak point -- read <a href=\"/blog/supabase-storage-buckets-probably-public\">Why Your Supabase Storage Buckets Are Probably Public</a> to check that attack surface too.</p>\n\n<p>The fix is not to stop using AI-generated code. The fix is to verify the security posture of what gets generated. Check your RLS. Check your policies. Check your indexes. Or let a tool check them for you.</p>\n\n<p><strong><a href=\"/login\">Scan your Supabase project for RLS misconfigurations --></a></strong></p>",
  },
  {
    slug: "supabase-storage-buckets-probably-public",
    title: "Why Your Supabase Storage Buckets Are Probably Public",
    description:
      "Most Supabase storage buckets are accidentally public. Learn how to audit your bucket permissions, fix common misconfigurations, and lock down file access.",
    category: "Storage",
    publishedAt: "2026-03-20",
    keywords: [
      "supabase storage security",
      "supabase public bucket",
      "supabase storage policies",
    ],
    body: "<p>Let us start with a question: if someone guessed the URL to a file in your Supabase storage bucket, could they download it?</p>\n\n<p>If you are using a public bucket -- and there is a good chance you are -- the answer is yes. No authentication required. No tokens. No permissions check. Just a URL and a browser.</p>\n\n<p>This is not a bug. This is how public buckets are designed to work. This is part of a broader <a href=\"/blog/state-of-supabase-security-2026\">Supabase security problem we documented</a>. The problem is that public is the path of least resistance, and many applications are storing files that should not be public at all.</p>\n\n<p>For a broader view of Supabase security challenges, including RLS and API key exposure, see <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a>.</p>\n\n<h3>Public vs. Private: What the Labels Actually Mean</h3>\n\n<p>Supabase Storage buckets come in two flavors:</p>\n\n<p><strong>Public buckets</strong> serve files at a predictable, unauthenticated URL:</p>\n\n<pre><code class=\"language-text\">https://{project-ref}.supabase.co/storage/v1/object/public/{bucket}/{path}</code></pre>\n\n<p>Anyone with this URL can download the file. There is no access check on reads. The file is effectively on the open internet.</p>\n\n<p>Upload, delete, move, and copy operations still require authentication and are controlled by RLS policies on the <code>storage.objects</code> table. But reads? Completely open.</p>\n\n<p><strong>Private buckets</strong> enforce RLS policies on <em>all</em> operations, including reads. To access a file, you either need a valid JWT (authenticated request) or a signed URL (temporary, time-limited access link).</p>\n\n<h3>Why Developers Choose Public Buckets</h3>\n\n<p>Public buckets are simpler. That is the entire reason.</p>\n\n<p>With a public bucket: no need to generate signed URLs, no need to handle URL expiration and refresh, no need to proxy requests through a backend. Image tags and file links work with plain URLs. Less code, fewer moving parts, faster development.</p>\n\n<p>With a private bucket: every file access requires either a signed URL (which expires) or an authenticated request. You need token management on the client side. More code, more complexity, more things that can break.</p>\n\n<p>When a developer (or an AI assistant) is trying to get file uploads working, the public bucket approach gets them to it-works faster. And in vibe coding, it-works is usually where development stops.</p>\n\n<h3>What Is Actually at Risk</h3>\n\n<p>The risk depends on what you are storing. Consider what typically ends up in Supabase storage buckets:</p>\n\n<ul>\n<li><strong>User profile photos:</strong> Often contain EXIF metadata with GPS coordinates, device info, and timestamps.</li>\n<li><strong>Document uploads:</strong> Resumes, contracts, identification documents, invoices.</li>\n<li><strong>Application assets:</strong> Screenshots, exports, reports that may contain sensitive data.</li>\n<li><strong>User-generated content:</strong> Posts, images, files shared within the application's context.</li>\n</ul>\n\n<p>The question is not can someone access the file but if someone accessed every file in this bucket, would that be a problem?</p>\n\n<h3>The Enumeration Problem</h3>\n\n<p>Public bucket URLs follow a predictable pattern. If an attacker knows one file URL:</p>\n\n<pre><code class=\"language-text\">https://abcdefgh.supabase.co/storage/v1/object/public/avatars/user_123.jpg</code></pre>\n\n<p>They can guess others:</p>\n\n<pre><code class=\"language-text\">https://abcdefgh.supabase.co/storage/v1/object/public/avatars/user_124.jpg\nhttps://abcdefgh.supabase.co/storage/v1/object/public/avatars/user_125.jpg</code></pre>\n\n<p>If your file naming scheme is sequential or based on predictable identifiers, enumeration is trivial. Even with UUIDs, if a file URL leaks through browser history, referrer headers, or server logs, it becomes permanently accessible.</p>\n\n<h3>How to Check Your Buckets</h3>\n\n<p><strong>Step 1: List your buckets and their visibility.</strong></p>\n\n<pre><code class=\"language-sql\">SELECT id, name, public FROM storage.buckets;</code></pre>\n\n<p>Any bucket where <code>public = true</code> serves files without authentication.</p>\n\n<p><strong>Step 2: Check what is in your public buckets.</strong></p>\n\n<pre><code class=\"language-sql\">SELECT bucket_id, name, created_at, metadata\nFROM storage.objects\nWHERE bucket_id IN (\n  SELECT id FROM storage.buckets WHERE public = true\n)\nORDER BY created_at DESC LIMIT 50;</code></pre>\n\n<p><strong>Step 3: Test access.</strong> Open a private/incognito browser window and try accessing a file URL directly. If it downloads, anyone can access it.</p>\n\n<h3>How to Fix It</h3>\n\n<h4>Option 1: Switch to a Private Bucket</h4>\n\n<p>You cannot change a bucket visibility after creation. Create a new private bucket, move files, update your application, and delete the old public bucket.</p>\n\n<pre><code class=\"language-sql\">INSERT INTO storage.buckets (id, name, public)\nVALUES ('user-uploads-private', 'user-uploads-private', false);</code></pre>\n\n<p>Then add RLS policies:</p>\n\n<pre><code class=\"language-sql\">CREATE POLICY &quot;Users can upload own files&quot;\n  ON storage.objects FOR INSERT\n  WITH CHECK (\n    bucket_id = 'user-uploads-private'\n    AND auth.uid()::text = (storage.foldername(name))[1]\n  );\n\n<p>CREATE POLICY &quot;Users can view own files&quot;   ON storage.objects FOR SELECT   USING (     bucket_id = 'user-uploads-private'     AND auth.uid()::text = (storage.foldername(name))[1]   );</p>\n\n<p>CREATE POLICY &quot;Users can delete own files&quot;   ON storage.objects FOR DELETE   USING (     bucket_id = 'user-uploads-private'     AND auth.uid()::text = (storage.foldername(name))[1]   );</code></pre></p>\n\n<p>Note: these storage RLS policies follow the same patterns as database table policies. If you are unfamiliar with USING vs WITH CHECK, see <a href=\"/blog/5-rls-mistakes-every-vibe-coder-makes\">5 RLS Mistakes Every Vibe Coder Makes</a> for a detailed breakdown.</p>\n\n<h4>Option 2: Use Signed URLs</h4>\n\n<p>For private buckets, generate time-limited signed URLs on the server side:</p>\n\n<pre><code class=\"language-javascript\">const { data, error } = await supabase.storage\n  .from('user-uploads-private')\n  .createSignedUrl('path/to/file.pdf', 3600); // expires in 1 hour</code></pre>\n\n<p>Set expiration times appropriate for your use case: short (5-15 minutes) for display purposes, longer (1-24 hours) for download links.</p>\n\n<h4>Option 3: Organize with Folder-Based Policies</h4>\n\n<p>Structure your storage so that file paths include the user ID, then write RLS policies that match the folder structure.</p>\n\n<h3>When Public Buckets Are Fine</h3>\n\n<p>Not every bucket needs to be private. Public buckets are appropriate for:</p>\n\n<ul>\n<li><strong>Marketing assets:</strong> Logos, product screenshots, social media images</li>\n<li><strong>Static content:</strong> Blog post images, documentation illustrations</li>\n<li><strong>Intentionally public resources:</strong> Open datasets, public downloads</li>\n</ul>\n\n<p>The rule of thumb: if the file would be fine on a public website, a public bucket is fine. If the file belongs to a specific user or contains any personal data, use a private bucket.</p>\n\n<h3>The Vibe Coding Connection</h3>\n\n<p>This issue is amplified by AI-generated code. When you ask an AI to add file upload to your app, the generated code typically creates a bucket (often public), implements the upload function, stores the public URL in the database, and renders the file using the public URL directly.</p>\n\n<p>The result works. Files upload and display correctly. The AI has solved the stated problem. What it has not done is evaluate whether the files being uploaded warrant access control. This is the same pattern we see across the board with vibe-coded Supabase apps -- <a href=\"/blog/state-of-supabase-security-2026\">The State of Supabase Security in 2026</a> covers the full picture.</p>\n\n<h3>Action Items</h3>\n\n<ol>\n<li><strong>Audit your buckets now.</strong> Run the SQL query above. Know which buckets are public.</li>\n<li><strong>Review the contents.</strong> Determine whether public access is appropriate for the files stored in each bucket.</li>\n<li><strong>Migrate sensitive data to private buckets.</strong> Create new private buckets, move files, update your application code.</li>\n<li><strong>Use signed URLs for private file access.</strong> Set appropriate expiration times.</li>\n<li><strong>Add storage policies.</strong> Use folder-based RLS policies that scope access to the file owner.</li>\n<li><strong>Automate the check.</strong> Include storage bucket auditing in your security review process, or <a href=\"/login\">run a free scan to check your storage configuration automatically</a>.</li>\n</ol>\n\n<p>Your users trust you with their files. Make sure that trust is warranted.</p>\n\n<p><strong><a href=\"/login\">Get a free security scan of your Supabase project --></a></strong></p>",
  },
];

export function getAllPosts(): readonly BlogPost[] {
  return posts.toSorted(
    (a, b) =>
      new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime(),
  );
}

export function getPostBySlug(slug: string): BlogPost | undefined {
  return posts.find((p) => p.slug === slug);
}

export function getPostsByCategory(
  category: string,
): readonly BlogPost[] {
  return posts
    .filter((p) => p.category === category)
    .toSorted(
      (a, b) =>
        new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime(),
    );
}

export function getAllSlugs(): readonly string[] {
  return posts.map((p) => p.slug);
}
